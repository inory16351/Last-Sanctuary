# -*- coding: utf-8 -*-
"""아루(캐릭터 9008) 모션 시트 → 프레임 분해.

원본
----
``<볼트>/리소스/sprites/Aru_asset_01.png``  (1536x1024 · **RGBA**)  ← 본체 전부 + 분리 이펙트
``<볼트>/리소스/sprites/Aru_asset_02.png``  (1536x1024 · RGB)       ← 「회복 받은 아군 이펙트」 별지
``<볼트>/리소스/sprites/Aru_dawn_asset.png``                        ← 골렘. **다른 유닛이라 다른 스크립트**

★★ 2026-08-20 — **시트가 교체되어 전부 다시 쟀다**
--------------------------------------------------
유저가 볼트에 새 판본을 올렸다(볼트 커밋 `1f9d5e0` 「테이블 수정」). 바뀐 것이 셋이다:

1. ``_01`` 이 <b>RGB → 알파 PNG</b> 로 바뀌었다. 아르세니아·카이론과 같은 교체다(120-11절) —
   :func:`skin_sheet.load_sheet` 는 이미 알파를 정본으로 읽으므로 <b>읽기는 그대로 된다.</b>
2. ``_01`` 의 <b>배치가 완전히 달라졌다.</b> 옛 판본은 «왼쪽 단 = 몸통 · 오른쪽 단 = 연출» 두 단이고
   좌우를 미러로 만들었는데, 새 판본은 <b>좌/우를 따로 그려 놓았다</b>(이동·근거리·원거리·마법·
   회복·스킬 1·스킬 2 전부). 그래서 옛 좌표표는 <b>한 줄도 쓸 수 없다.</b>
3. 옛 ``_02`` (``_01`` 과 md5 가 같던 군더더기)가 <b>지워지고</b>, 옛 ``_03`` (회복 별지)이
   <b>``_02`` 로 내려왔다.</b> md5 로 확인했다 — 별지 <b>내용은 한 픽셀도 안 바뀌었으므로</b>
   그 줄의 좌표(``y 112~750`` · 라벨 ``771~788``)는 <b>그대로 쓴다.</b>

★★ **미러를 버렸다** — 이것이 이번 개편의 요점
----------------------------------------------
옛 판본은 오른쪽만 그려져 있어 :data:`Spec.default_side` 로 뒤집었다. 새 판본은 좌/우가
<b>다른 그림</b>이다(예: 이동 좌 줄은 랜턴을 <b>왼손</b>에 들고, 우 줄은 <b>오른손</b>에 든다).
미러로 만들면 랜턴이 반대 손으로 옮겨 간다 — 그래서 :data:`Row.side` 로 <b>줄마다 방향을 박고</b>
한 폴더(``Walk``)에 좌·우를 함께 굽는다. 카이론·아르세니아와 같은 방식이다.

⚠⚠ **근거리는 좌 4장 · 우 5장이다** — 원화가 그렇게 그려져 있다
---------------------------------------------------------------
좌우 장수가 다른 줄은 이 프로젝트에서 처음이다. 눈으로 세고, <b>두건 안쪽의 «아주 어두운
픽셀» 덩어리를 세어</b> 두 번 확인했다(낫 궤적은 파랗고 밝아 그 판정에 안 걸린다):

    근거리 좌 : 덩어리 4개  (x 62~188 · 215~255 · 301~364 · 391~458)
    근거리 우 : 덩어리 4개인데 <b>첫 덩어리가 두 몸통</b>(59~204) — 눈으로는 5장이다

:class:`CharacterSkinSO` 는 ``attackRight``/``attackLeft`` 를 <b>따로</b> 들고 있으므로
장수가 달라도 문제가 없다. ⚠ 다만 다음에 시트가 또 바뀌면 <b>여기를 먼저 의심할 것</b>.

★ **칸 가르기는 전부 ``bounds``(손으로 잰 값)다**
------------------------------------------------
자동 세 가지를 다 대 보고 내린 결론이다:

* ``gaps``     — 몸통이 <b>서로 닿아</b> 빈 열이 안 생긴다(대기 8장에 빈 열은 5곳뿐이었다).
* ``clusters`` — 낫 궤적·마법진이 옆 칸까지 뻗어 <b>한 덩어리</b>로 잡힌다.
* ``feet``     — 망토 자락이 <b>바닥을 따라 이어져</b> 발밑 띠에서도 덩어리가 붙는다
  (원거리 좌 2조각 · 회복 3조각 · 스킬 1 우 2조각 — 전부 장수 미달로 죽었다).

그래서 히스톤·카이론과 같은 결론으로 갔다 — <b>눈으로 재서 박는다</b>. 값은
① `Tools/sheet_labels.py` 로 제목 딱지 9개를 찾고 ② `Tools/sheet_band.py` 로 줄마다 3배
확대해 격자를 읽고 ③ 위의 «어두운 덩어리» 로 검산해 정했다.
⚠ 원화를 다시 받으면 이 표는 전부 무효다.

무엇이 어디로 가나
------------------
| 폴더 | 장수 | 스킨 칸 | 원화 |
|---|---:|---|---|
| ``Char/Idle``          | 8   | ``idleRight``·``idleLeft`` | 대기 한 줄 → **미러**(방향이 없는 자세다) |
| ``Char/Walk``          | 8+8 | ``walkRight``·``walkLeft`` | 이동 좌/우 |
| ``Char/MeleeAttack``   | 4+5 | ``attackRight``·``attackLeft`` | 근거리 좌/우 (⚠ 장수가 다르다) |
| ``Char/RangedAttack``  | 3+3 | ``rangedRight``·``rangedLeft`` | 원거리 좌/우 |
| ``Char/MagicAttack``   | 3+3 | ``magicRight``·``magicLeft`` | 마법 좌/우 |
| ``Char/Heal``          | 4+4 | ``healRight``·``healLeft`` | 회복 좌/우 |
| ``Char/Skill1``        | 3+3 | ``skill1Right``·``skill1Left`` | 스킬 1 「도움의 손길」(80022) |
| ``Char/Skill2``        | 4+4 | ``skill2Right``·``skill2Left`` | 스킬 2 「강림」(80024) |
| ``Char/Projectile``    | 3   | ``projectileFrames`` | 분리 애셋 줄의 구슬 3장 |
| ``Char/MuzzleFlash``   | 1   | ``muzzleFlashFrames`` | 같은 줄 맨 앞의 <b>랜턴</b> |
| ``Char/ImpactMagic``   | 1   | ``magicImpactFrames`` | 같은 줄 마지막의 <b>큰 구슬(고리 달린 폭발)</b> |
| ``Char/MeleeTravelFx`` | 4   | ``meleeTravelFrames`` | <b>낫 궤적 4장</b> — 새 시트가 처음 준 것 |
| ``Char/Skill1Fx``      | 5   | ``skill1Fx`` | 마법진 → 고리 → 소용돌이 → 사슬 → 사슬+소용돌이 |
| ``Char/Skill2Fx``      | 3   | ``skill2Fx`` | 바닥 마법진 3장(빛기둥이 자란다) |
| ``Char/HealFx``        | 8   | ``healFxFrames`` | **별지 ``_02``** (아래 ★) |

★ **회복 이펙트는 별지 ``_02`` 를 쓴다.** ``_01`` 아래쪽에도 금빛 반짝임·깃털·날개가 있지만
  그쪽은 <b>«시술자 쪽 잔광» 조각 모음</b>이고 두 소줄로 겹쳐 그려져 있어 프레임으로 못 가른다.
  별지는 같은 이펙트를 <b>마네킹 없이 8장 한 줄</b>로 그려 둔 판이다 — 옛 판본에서 쓰던 그 줄이다.

⚠ **굽지 않고 남기는 것** (Chiron 처럼 ``Unused_`` 로 굽지 않았다 — 이유를 적어 둔다):

* ``_01`` y 855~1013 · x 17~582 — 금빛 반짝임·깃털·날개. 위에 적은 대로 <b>두 소줄이 겹쳐</b>
  있어 ``expect`` 를 박을 수 없다. 별지에 같은 이펙트가 있으므로 <b>잃는 것이 없다.</b>
* ``_01`` y 855~1013 · x 591~871 — 작은 반짝임·사슬 고리·랜턴 두 개·왕관 고리. 원화 참조용
  소품이고 어느 슬롯에도 대응이 없다.
* 원거리·마법 줄 안에 들어 있는 <b>구슬·랜턴 복제</b>(x 806~1000 · 1321~1518) — 분리 애셋 줄에
  같은 것이 <b>더 크고 깨끗하게</b> 있다. 그쪽을 쓴다.

★★ 2026-08-21 — **크기 정규화를 켰다**(``scale_metric="head"``). 그 전 판단이 틀렸다
------------------------------------------------------------------------------
유저 리포트: *"아루 스킨만 크기가 작아진다"*. **시트가 대기 줄만 크게 그려 놓았다** —
위의 옛 주석("시트가 모든 줄을 같은 크기로 그렸다")은 <b>상자 크기를 안 재고 내린 추측</b>이었다.
:func:`char_sheet.report_body_size` 가 이미 경고를 찍고 있었는데 «자세일 수 있다» 로 넘겼다.

실측(같은 발 높이로 아래를 맞춰 겹쳐 본 것과 일치):

| 줄 | 상자 높이 | 대기 대비 |
|---|---:|---:|
| 대기   | **122 px** | 기준 |
| 이동   | 82 px  | **−33%** |
| 근거리 | 90 px  | −26% |
| 원거리 | 84 px  | −31% |
| 마법   | 88 px  | −28% |
| 회복   | 82 px  | −33% |
| 스킬 1 | 98 px  | −20% |
| 스킬 2 | 116 px | −5% |

★ 이것이 곧 <b>인게임에서 아루만 작아 보이는 이유</b>다. 몸집 배율은
:data:`CharacterSkinSO.contentSizeTiles` = <b>대기 원화 하나</b>로 정해지고
(`Tools/measure_skin_tiles.py`), 캐릭터는 ``renderHeightTiles`` 2.15 로 그려진다:

    대기   122px → 2.15 타일   (기준이므로 정확히 맞는다)
    이동    82px → **1.45 타일**  ← 걷는 순간 32% 줄어든다

즉 «대기일 때만 제 크기»였다. 말파스(113절 ②)·시그리드(123절 ④)와 <b>같은 버그</b>다.

⚠ 기준은 <b>머리 면적</b>(``"head"``)을 쓴다 — «상자 높이»(``"height"``)로 재면
이동 x1.485 가 나오는데, 아루의 이동 원화는 <b>몸을 앞으로 기울인다</b>(머리−발 +17px)
그래서 상자가 자세 때문에도 짧아진 것이라 그 값은 <b>과교정</b>이다.
머리로 재면 x1.279 — 시그리드가 같은 이유로 머리를 택했다(:func:`skin_sheet.head_pixels`).

⚠ 정규화의 기준 줄은 <b>대기</b>다(:data:`Spec.scale_reference`). 대기를 기준으로 두면
``contentSizeTiles`` 가 <b>안 바뀌므로</b> 캐릭터 사이 크기(2.15 타일)도 그대로다 —
바뀌는 것은 «모션이 바뀔 때 커졌다 작아졌다» 하는 것뿐이다.

사용법:  py -3 Tools/aru_skin_build.py
다음:    유니티 메뉴 **LastSanctuary/스킨/원화 폴더로 스킨 에셋 만들기**
        그 다음 `py -3 Tools/measure_skin_tiles.py`
"""

import os

from vault_path import VAULT, PROJECT
from char_sheet import Row, Spec, run

ART = os.path.join(VAULT, "리소스", "sprites")
SRC01 = os.path.join(ART, "Aru_asset_01.png")
SRC02 = os.path.join(ART, "Aru_asset_02.png")

DST = os.path.join(PROJECT, "Assets", "_Project", "Art", "Char_Asset",
                   "Char_Asset_Aru", "Char")

C = ("clusters",)                       # 덩어리 하나뿐인 이펙트
LB = ("labels", 771, 788)               # 별지의 프레임 번호 줄

# ──────────────────────────────────────────────────────────────────────────
#   손으로 잰 칸 경계. 값은 «칸 시작 x» 를 이어 적고 <b>마지막에 끝+1</b> 을 붙인다
#   (:func:`char_sheet.cells_of` 의 ``bounds`` 규칙).
#
#   ⚠ 좌·우가 <b>같은 격자가 아니다</b> — 원화가 줄마다 자유롭게 그려져 있어서
#     한쪽 값을 다른 쪽에 쓰면 몸통이 반토막 난다. 그래서 <b>줄마다 따로</b> 적었다.
#     (카이론은 좌·우가 같은 격자여서 한 값을 둘에 썼다 — 시트마다 다르다.)
# ──────────────────────────────────────────────────────────────────────────
IDLE_B = ("bounds", [28, 110, 182, 256, 355, 445, 532, 623, 728])

WALK_L_B = ("bounds", [845, 913, 998, 1081, 1167, 1252, 1335, 1431, 1518])
WALK_R_B = ("bounds", [838, 912, 989, 1074, 1160, 1250, 1339, 1427, 1514])

#: ⚠ 좌 4장 · 우 5장 (맨 위 ⚠⚠).
MELEE_L_B = ("bounds", [56, 200, 266, 365, 501])
MELEE_R_B = ("bounds", [56, 115, 213, 304, 405, 503])

RANGED_L_B = ("bounds", [553, 636, 722, 801])
RANGED_R_B = ("bounds", [553, 636, 722, 803])

MAGIC_B = ("bounds", [1050, 1141, 1226, 1320])

HEAL_L_B = ("bounds", [60, 155, 272, 345, 470])
HEAL_R_B = ("bounds", [60, 146, 250, 358, 470])

SKILL1_L_B = ("bounds", [535, 642, 732, 831])
SKILL1_R_B = ("bounds", [535, 630, 736, 831])

SKILL2_L_B = ("bounds", [1050, 1156, 1263, 1384, 1522])
SKILL2_R_B = ("bounds", [1050, 1161, 1264, 1392, 1522])

#: 분리 애셋 줄 — 구슬 3장(랜턴·큰 구슬은 따로 뺀다).
PROJ_B = ("bounds", [68, 159, 230, 324])
#: 낫 궤적 4장. ⚠ 첫 칸(515~621)은 «큰 궤적 안에 작은 궤적» 이 겹쳐 그려진 <b>한 장</b>이다 —
#:   둘로 가르면 두 쪽이 다 반토막 난다.
MELEE_FX_B = ("bounds", [513, 630, 716, 806, 1025])
SKILL1_FX_B = ("bounds", [1039, 1129, 1200, 1308, 1402, 1520])
SKILL2_FX_B = ("bounds", [872, 1075, 1355, 1530])

SPEC = Spec(
    title="아루",
    sources={"01": SRC01, "02": SRC02},
    dst_root=DST,
    skin_spec={
        "skinAssetName": "Skin_Aru",
        "displayName": "아루",
        "framesPerSecond": "10",
        "attackFramesPerSecond": "14",
        # 날아가는 마법 구슬 원화가 실재한다.
        "groundImpactOnly": "0",
        "projectileWidthTiles": "0.9",
        "impactWidthTiles": "1.4",
    },
    # ★★ 제목 딱지 — `Tools/sheet_labels.py` 실측값 중 <b>글자가 읽히는 9개</b>만.
    #   자동 탐지는 12개를 냈는데 셋(y535·y633·y651)은 <b>랜턴·날개의 어두운 덩어리</b>가
    #   딱지처럼 보인 오검출이다 — 지우면 그림에 구멍이 난다.
    #   ⚠ 「좌」·「우」 딱지는 지우지 않는다 — 밴드 x0 를 딱지 밖으로 잡아 피했다.
    erase=(
        (   8,  43,   14,  153),   # 대기 모션
        (   8,  42,  795,  933),   # 이동 모션
        ( 211, 246,   14,  192),   # 근거리 공격 (낫)
        ( 211, 246,  499,  699),   # 원거리 공격 (랜턴)
        ( 210, 246, 1009, 1192),   # 마법 공격 (랜턴)
        ( 449, 483,   14,  154),   # 회복 모션
        ( 449, 488,  498,  822),   # 스킬 1 - 끌어당김 (⚠ 화살 장식이 4px 더 내려온다)
        ( 448, 484, 1009, 1268),   # 스킬 2 - 성역
        ( 699, 733,   14,  269),   # 투사체 / 이펙트 (분리 애셋)
    ),
    rows=[
        # ── 몸통 ─────────────────────────────────────────────────────
        # ★ 대기만 <b>한 줄</b>이다(좌/우 딱지가 없다) — 미러로 반대쪽을 만든다.
        Row("Idle",        "body",  44, 200,   28,  727, IDLE_B, 8),

        Row("WalkLeft",    "body",  44, 124,  845, 1517, WALK_L_B, 8,
            folder="Walk", side="Left"),
        Row("WalkRight",   "body", 125, 209,  838, 1513, WALK_R_B, 8,
            folder="Walk", side="Right"),

        # ⚠⚠ 이 줄의 밴드는 <b>위·아래 양쪽을 실측으로 죄었다</b>:
        #   · y1=343 이면 <b>아래 우 줄의 낫 조각</b>이 밑단에 파란 띠로 들어온다
        #     (좌 줄 발밑은 y336 에서 끝나고 337~343 은 우 줄 후광이다) → 338
        #   · y0=247 이면 <b>3번 프레임의 낫 궤적</b>이 잘린다(y241 부터 그림이다) → 240
        Row("MeleeLeft",   "body", 240, 338,   56,  500, MELEE_L_B, 4,
            folder="MeleeAttack", side="Left"),
        Row("MeleeRight",  "body", 344, 436,   56,  502, MELEE_R_B, 5,
            folder="MeleeAttack", side="Right"),

        Row("RangedLeft",  "body", 249, 345,  553,  800, RANGED_L_B, 3,
            folder="RangedAttack", side="Left"),
        Row("RangedRight", "body", 346, 430,  553,  802, RANGED_R_B, 3,
            folder="RangedAttack", side="Right"),

        Row("MagicLeft",   "body", 247, 352, 1050, 1319, MAGIC_B, 3,
            folder="MagicAttack", side="Left"),
        Row("MagicRight",  "body", 353, 439, 1050, 1319, MAGIC_B, 3,
            folder="MagicAttack", side="Right"),

        Row("HealLeft",    "body", 488, 586,   60,  469, HEAL_L_B, 4,
            folder="Heal", side="Left"),
        Row("HealRight",   "body", 587, 677,   60,  469, HEAL_R_B, 4,
            folder="Heal", side="Right"),

        Row("Skill1Left",  "body", 489, 595,  535,  830, SKILL1_L_B, 3,
            folder="Skill1", side="Left"),
        Row("Skill1Right", "body", 596, 692,  535,  830, SKILL1_R_B, 3,
            folder="Skill1", side="Right"),

        # ⚠ y0 이 490 이면 <b>4번 프레임의 날개 끝</b>이 잘린다(실측: y447 까지 솟는다).
        #   제목 딱지는 x1009~1268 이라 x1380+ 의 날개와 겹치지 않고, 어차피 지운다.
        #   ⚠⚠ <b>크기 정규화 기준에서 뺐다</b>(``scale=False`` · 2026-08-21).
        #     「강림」은 빛기둥과 날개가 크게 펼쳐지는 줄이라 머리 판정이 흔들린다 —
        #     좌 x0.855 · 우 x0.971 로 <b>좌우가 12% 어긋나</b> 방향을 바꿀 때 몸이 튄다.
        #     원화가 일부러 큰 줄이기도 하다(아르세니아 「천사 강림」과 같은 판단).
        Row("Skill2Left",  "body", 445, 604, 1050, 1521, SKILL2_L_B, 4,
            folder="Skill2", side="Left", scale=False),
        Row("Skill2Right", "body", 605, 722, 1050, 1521, SKILL2_R_B, 4,
            folder="Skill2", side="Right", scale=False),

        # ── 분리 애셋(연출) ──────────────────────────────────────────
        # ★ 2026-08-21 — 이펙트 밴드 실측 (`Tools/sheet_fxband.py`). 유저: *"이펙트가 안
        #   잘리게 해 윗부분 살짝 잘렸자나"*. 값은 «제목 딱지 판과 이웃 줄 사이에서
        #   이펙트가 실제로 차지하는 범위» 다 — 딱지는 가로 150px 이상 이어지는 한 덩어리라
        #   둥근 이펙트와 갈린다. 굽고 나서 눈으로 확인했다.
        Row("MuzzleFlash",   "fx", 740,  845,   25,   70, C, 1),
        Row("Projectile",    "fx", 740,  845,   68,  323, PROJ_B, 3),
        Row("ImpactMagic",   "fx", 740,  845,  324,  471, C, 1),
        Row("MeleeTravelFx", "fx", 728,  849,  513, 1024, MELEE_FX_B, 4),
        Row("Skill1Fx",      "fx", 723,  848, 1039, 1519, SKILL1_FX_B, 5),
        Row("Skill2Fx",      "fx", 850, 1013,  872, 1529, SKILL2_FX_B, 3),

        # ── 별지 ─────────────────────────────────────────────────────
        Row("HealFx",        "fx",  95,  751,   10, 1525, LB, 8, src="02"),
    ],
    no_direction=("MuzzleFlash", "Projectile", "ImpactMagic",
                  "MeleeTravelFx", "Skill1Fx", "Skill2Fx", "HealFx"),
    scale_reference="Idle",
    # ★★ 머리 면적으로 줄마다 크기를 맞춘다 — 맨 위 「크기 정규화를 켰다」.
    scale_metric="min",
)

#: 이펙트 줄의 **목표 높이(px)**.
#:
#: ★ ``HealFx`` 만 있는 이유 — 그 줄만 <b>별지(``_02``)</b>에서 오고, 그 시트는 같은 이펙트를
#:   <b>639px 높이</b>로 크게 그려 뒀다. 그대로 구우면 PPU 64 기준 <b>10타일</b> 짜리 그림이
#:   아군 발밑에 깔린다(엘린의 회복 이펙트는 109px · 대기의 1.2배다).
#:   :meth:`CombatProjectileFx.PlayHeal` 은 ``impactWidthTiles`` 가 없으면 <b>원본 크기 그대로</b>
#:   그리므로, 크기를 여기서 정해 두는 것이 가장 확실하다.
FX_TARGET_HEIGHT = {"HealFx": 120}


def main():
    run(SPEC, fx_target_height=FX_TARGET_HEIGHT)


if __name__ == "__main__":
    main()
