# -*- coding: utf-8 -*-
"""불칸(캐릭터 9011) 모션 시트 → 프레임 분해.

원본: ``<볼트>/리소스/sprites/Vulcan_asset.png`` (1536x1024)

★★ 2026-08-20 — **시트가 통째로 교체되어 전부 다시 쟀다**
--------------------------------------------------------
유저 지시: *"아르세니아·카이론 아루/불칸 새로 넣은 에셋으로 스킨 재제작 …
이펙트가 짤리거나 캐릭터의 크기/피벗이 어긋나지 않도록"*.

<b>레이아웃이 바뀌었다</b> — 옛 시트는 «오른쪽 원화 한 벌 + 미러» 였는데,
새 시트는 <b>모션마다 좌·우가 따로 그려져 있다</b>(「좌」·「우」 딱지가 줄마다 붙어 있다).
그래서 모든 몸통 줄이 :attr:`char_sheet.Row.side` 를 갖는다 — <b>미러하지 않는다.</b>
미러하면 오른쪽 원화를 뒤집은 것이 왼쪽 원화와 겹쳐 «두 벌» 이 되고, 어느 쪽이
재생되는지는 파일 이름 순서에 달리게 된다.

옛 좌표는 <b>한 줄도 맞지 않는다</b>(120-11절의 규칙: 원화가 교체되면 좌표표는 전부 무효).

★★ **딱지를 지우고 시작한다**
-----------------------------
새 시트는 제목 딱지와 「좌」·「우」 딱지가 <b>프레임과 같은 y</b> 에 있다. 예를 들어
「스킬 1 타격 이펙트」 딱지는 y 426~452 이고 그 아래 프레임은 y 432 부터 시작한다 —
밴드를 딱지 아래로 미루면 <b>프레임 윗부분이 잘리고</b>, 딱지를 품으면 <b>검은 판때기가
프레임에 박혀</b> 나온다. 어느 쪽도 안 된다.

→ :attr:`char_sheet.Spec.erase` 로 <b>딱지만 정확히 지운다</b>. 좌표는
  ``Tools/sheet_erase.py`` 가 «줄마다 어두운 비율» 로 재 준 값이다(눈대중이 아니다).
  ⚠ 1px 이라도 길게 잡으면 그 아래 머리가 잘린다 — 메테오 딱지를 «543~575» 로
  잡았다가 실제가 <b>543~567</b> 임을 그 도구로 알아냈다.

★★ **칸은 «발밑»으로 가른다** — ``span`` 도 ``clusters`` 도 못 쓴다
------------------------------------------------------------------
이 시트는 프레임이 <b>서로 닿아 있고</b> 이펙트가 <b>몸통과 겹쳐</b> 그려져 있다.
세 방법을 다 써 보고 실패한 기록(전부 실측):

* ``gaps``     — 푸른 칼자국이 칸 사이를 메워 5칸이 <b>1~3칸</b>이 된다
* ``clusters`` — 개수는 맞을 때가 있지만 «가장 얇은 곳» 에서 끊어 <b>몸통 안쪽</b>으로
  경계가 파고든다(마법 4번 칸이 x399 에서 끊겨 몸이 잘렸다)
* ``span``     — 줄 끝의 «손을 떠난 탄» 까지 나눗셈에 들어가 <b>모든 칸이 밀린다</b>.
  게다가 이 시트는 간격이 고르지도 않다(94·93·92·99·106·106·109)

→ :func:`skin_sheet.cells_by_feet` 를 새로 만들었다. <b>밴드 아래쪽 18% 만 본다</b> —
  몸통은 땅을 딛고 이펙트는 공중에 뜨므로 <b>발만 남고</b>, 발은 프레임마다 깨끗하게
  떨어져 있다. 「손을 떠난 탄」이 <b>저절로 빠지는</b> 덤까지 있다.

★★ **밴드가 옆 줄을 침범하면 «캐릭터가 작아진다»**
--------------------------------------------------
유저 리포트: *"캐릭터 몸통이랑 피벗 맞춰 동일하게 모션 진행시 커졌다 작아졌다 하지 않게"*.

처음에 스킬 3 밴드를 y 796 부터 잡았는데, <b>회복 「우」 줄이 y 803 까지 내려온다</b>.
그래서 스킬 3 프레임마다 <b>머리 위에 회복 빛 조각</b>이 딸려 들어왔고, 캔버스 세로가
<b>112px</b>(대기는 90px)로 부풀었다. 캔버스가 커지면 그 안의 몸이 <b>차지하는 비율이
줄어든다</b> — ``contentSizeTiles`` 로 계산한 배율은 대기 기준 한 값이므로, 결과적으로
<b>스킬 3 을 쓰는 동안 캐릭터가 20% 작아 보인다.</b> 그것이 «커졌다 작아졌다» 의 정체였다.

→ 모든 밴드를 <b>이펙트가 없는 깨끗한 열</b>에서 다시 쟀다(x 60~120 · 440~505 ·
  600~660 · 760~830 · 830~930 · 960~1032). 스킬 3 좌 줄은 <b>816</b> 부터다.
⚠ 반대 사고도 같이 잡았다 — 대기 줄을 38 부터 잡아 <b>머리 위 9px 가 잘리고</b> 있었다
  (실제 위끝은 27). 이동 「우」 줄도 2px 잘렸다.
★ 밴드를 고친 뒤 캔버스 세로가 <b>대기 99 · 나머지 78~99</b> 로 모였다 — 위 사고의 반대편,
  «몸통이 자세 때문에 짧아지는 것» 은 <b>연출이므로 건드리지 않는다</b>(웅크린 이동 원화).

무엇이 어디로 가나
------------------
| 폴더 | 장수 | 스킨 칸 | 근거 |
|---|---:|---|---|
| ``Idle`` | 6+6 | ``idleRight/Left`` | 좌·우 원화 따로 |
| ``Walk`` | 9+9 | ``walkRight/Left`` | 〃 |
| ``MeleeAttack`` | 5+5 | ``attackRight/Left`` | 〃 |
| ``RangedAttack`` | 4+4 | ``rangedRight/Left`` | 뒤 1칸은 «손을 떠난 탄» — ``take`` 로 뺀다 |
| ``MagicAttack`` | 4+4 | ``magicRight/Left`` | 〃 (뒤 1칸은 마법진) |
| ``Heal`` | 5+5 | ``healRight/Left`` | 전부 몸통이다 |
| ``Skill1`` | 4+4 | ``skill1Right/Left`` | **타오르는 분노**… 는 상시효과라 실은 <b>화염 세례</b>의 시전 모션이다(아래 ⚠) |
| ``Skill3`` | 5+5 | ``skill3Right/Left`` | 메테오 시전 |
| ``Projectile`` | 4 | ``projectileFrames`` | 파란 불덩이 |
| ``MuzzleFlash`` | 5 | ``muzzleFlashFrames`` | 지팡이 끝의 파란 섬광 |
| ``Impact`` · ``ImpactMagic`` | 5 · 5 | ``impactFrames`` · ``magicImpactFrames`` | <b>같은 그림</b>을 두 칸에 넣는다(시트가 «원거리 / 마법» 을 한 줄로 준다) |
| ``Skill1Projectile`` | 5 | ``skill1Projectile`` | 붉은 불덩이 |
| ``Skill1Fx`` | 5 | ``skill1Fx`` | 붉은 마법진 |
| ``Skill3Fx`` | 4 | ``skill3Fx`` | <b>메테오가 «땅에서 타는» 쪽</b>만 쓴다(아래 ★★) |
| ``HealFx`` | 5 | ``healFxFrames`` | 회복 대상에 뜨는 고리·기둥 |
| ``Unused_MeteorFall`` | 5 | — | 메테오가 «떨어지는» 쪽. 배선할 칸이 없다 |
| ``Unused_HealFxAlt`` | 4 | — | 회복 이펙트 두 번째 줄(날개·구슬 — 고를 수 있게 남긴다) |

★★ **메테오를 둘로 가른 이유** — 「메테오 이펙트」 구획은 <b>두 줄</b>이다: 위는 떨어지는
   불덩이, 아래는 <b>땅에서 타는 불과 파편</b>. ``skill3Fx`` 는 «지면 연출»(피해 범위에
   맞춰 늘려 그린다)이므로 <b>떨어지는 쪽을 넣으면 하늘의 불덩이가 바닥에 눌려</b> 나온다.
   그래서 <b>타는 쪽</b>을 배선하고 떨어지는 쪽은 ``Unused_`` 로 남겼다.
   ⚠ 두 줄이 x 1105~1164 구간에서 <b>서로 닿아 있다</b>(실측) — 그 열의 위 프레임 아래쪽과
   아래 프레임 위쪽이 몇 px 겹친다. 완전히 가를 방법은 원화에 틈을 두는 것뿐이다.

⚠ **사망 모션을 굽지 않는다** (유저 지시: *"사망 모션 넣지 않기 … 단, 골렘은 예외"*).
   이 시트에는 애초에 사망 줄이 없다.
⚠ **탈진 모션도 없다** — 유저가 *"불칸은 탈진 모션 필요 없으니까 빼고"* 라고 했고
   원화도 그렇게 왔다. 「불안정성」의 탈진은 <b>아르세니아만</b> 쓴다.
⚠ 크기 정규화는 **하지 않는다** — 시트가 「2x 확대 / 1x」 견본을 나란히 그려 두어
   <b>모든 줄이 같은 크기</b>임을 스스로 밝히고 있다. 정규화를 걸면 자세 차이를
   크기 차이로 잘못 읽어 오히려 흔들린다(카이론·아르세니아와 같은 판단).

사용법:  py -3 Tools/vulcan_skin_build.py
"""

import os

from vault_path import VAULT, PROJECT
from char_sheet import Row, Spec, run

SRC = os.path.join(VAULT, "리소스", "sprites", "Vulcan_asset.png")
DST = os.path.join(PROJECT, "Assets", "_Project", "Art", "Char_Asset",
                   "Char_Asset_Vulcan", "Char")

#: ★★ 몸통 줄은 <b>발밑</b>으로 가른다 — 이 시트는 이펙트가 몸통과 겹쳐 그려져 있어
#:   다른 방법이 전부 어긋난다(맨 위 ★ · `skin_sheet.cells_by_feet` 의 긴 주석).
F = ("feet",)
#: 「손을 떠난 탄」·「바닥 마법진」이 마지막 칸에 딸려 오는 줄 — <b>허리에서 끊는다</b>
#:   (`skin_sheet.cells_by_feet` 의 ``trim_tail``). 밴드 x1 을 줄마다 손으로 맞추던 것을
#:   대신한다(좌·우 값이 달라 손으로는 못 쫓는다).
FT = ("feet", None, None, True)
#: 이펙트 줄(오른쪽 단)은 발이 없으므로 덩어리로 가른다.
C = ("clusters",)

#: 총구 섬광 다섯 장의 칸 경계 — 위 ★★ 참조.
MUZZLE_B = ("bounds", [1085, 1160, 1252, 1345, 1450, 1500])

#: 왼쪽 단(모션)에서 「좌」·「우」 딱지 오른쪽으로 프레임이 시작하는 x.
L0, L1 = 54, 514        # 근거리·마법·스킬1 구획
R0, R1 = 564, 1032      # 원거리·회복 구획
FX0, FX1 = 1075, 1533   # 오른쪽 단(분리 이펙트)

#: ⚠ **발밑으로도 못 가르는 두 줄**은 경계를 박아 준다.
#:
#:   ``Skill3`` — 4번 칸이 «정면으로 서서 바닥에 불 고리를 그리는» 그림이라 <b>고리가
#:   발처럼 보인다</b>(좌 줄에서 7조각이 나왔다). 값은 <b>우 줄의 발</b>(5조각이
#:   깨끗하게 나왔다)에서 «발 사이 가운데» 로 계산한 것이고, 좌·우가 <b>같은 격자</b>라
#:   두 줄에 그대로 쓴다.
SK3_B = ("bounds", [544, 645, 729, 808, 934, 1033])
#:   ``HealRight`` — 2·3번 몸통이 파란 기둥으로 이어져 발이 하나로 붙는다(4조각).
#:   값은 같은 격자의 <b>좌 줄</b>(5조각)에서 계산했다.
HEAL_B = ("bounds", [564, 656, 747, 833, 925, 1033])

SPEC = Spec(
    title="불칸",
    sources={"01": SRC},
    dst_root=DST,
    skin_spec={
        "skinAssetName": "Skin_Vulcan",
        "displayName": "불칸",
        "framesPerSecond": "10",
        "attackFramesPerSecond": "14",
        "groundImpactOnly": "0",
        "projectileWidthTiles": "1.0",
        "impactWidthTiles": "1.6",
    },
    # ★★ 제목 딱지 · 「좌」「우」 딱지 — `Tools/sheet_erase.py` 실측값(맨 위 ★★).
    erase=(
        (   9,   35,   68,  181),   # 2x 확대 (견본 — 굽지 않는다)
        (   9,   35,  367,  494),   # 대기 모션
        (   9,   35, 1087, 1286),   # 투사체 / 이펙트 (분리 에셋)
        (  43,   68, 1081, 1304),   # 원거리 / 마법 투사체
        ( 204,  229, 1080, 1259),   # 원거리 / 마법 타격 이펙트
        ( 229,  255,   23,  211),   # 이동 모션
        ( 332,  357, 1079, 1269),   # 스킬 1 투사체
        # ⚠ 이 둘만 <b>419</b> 부터 지운다 — 딱지 위쪽 흐릿한 테두리(y420~423)가
        #   이동 「우」 줄의 발밑 띠에 걸려 <b>없는 칸을 하나 더</b> 만들었다(10칸).
        #   이동 줄의 그림은 y418 에서 끝나므로 419 부터는 지워도 잃는 것이 없다.
        ( 419,  450,   22,  251),   # 근거리 공격 모션
        ( 419,  450,  529,  791),   # 원거리 공격 모션
        ( 426,  452, 1079, 1217),   # 스킬 1 타격 이펙트
        ( 543,  567, 1079, 1273),   # 스킬 3 메테오 이펙트
        ( 607,  633,   22,  232),   # 마법 공격 모션
        ( 607,  633,  529,  695),   # 회복 모션
        ( 800,  825,   22,  234),   # 스킬 1 모션
        ( 802,  829,  513,  665),   # 스킬 3 모션
        ( 804,  831, 1079, 1235),   # 회복 대상 이펙트
        # ── 「좌」·「우」 딱지 ──
        (  59,   86,  352,  392),
        ( 154,  181,  352,  392),
        ( 275,  306,   18,   52),
        ( 358,  388,   18,   52),
        ( 470,  500,   18,   52),
        ( 540,  568,   18,   52),
        ( 472,  502,  528,  562),
        ( 540,  571,  528,  562),
        ( 652,  682,   18,   52),
        ( 730,  761,   18,   52),
        ( 654,  684,  528,  562),
        ( 734,  765,  528,  562),
        ( 845,  875,   18,   52),
        ( 926,  957,   18,   52),
        ( 848,  879,  513,  548),
        ( 935,  966,  513,  548),
    ),
    rows=[
        # ── 몸통 : 좌·우가 따로 그려져 있어 미러하지 않는다 ───────────
        Row("IdleLeft",    "body",  27, 125,  400, 1032, F, 6, folder="Idle", side="Left"),
        Row("IdleRight",   "body", 126, 226,  400, 1032, F, 6, folder="Idle", side="Right"),

        Row("WalkLeft",    "body", 252, 338,   54,  942, F, 9, folder="Walk", side="Left"),
        Row("WalkRight",   "body", 340, 421,   54,  950, F, 9, folder="Walk", side="Right"),

        Row("MeleeLeft",   "body", 445, 525,   L0,   L1, F, 5,
            folder="MeleeAttack", side="Left"),
        Row("MeleeRight",  "body", 526, 606,   L0,   L1, F, 5,
            folder="MeleeAttack", side="Right"),

        # ⚠⚠ 마지막 칸은 «손을 떠난 파란 탄» 이다. 발밑으로 세면 <b>칸 수는</b> 저절로 4가
        #   되지만(탄은 공중에 뜬다) <b>마지막 칸의 오른쪽 끝은 여전히 x1</b> 이라 탄이
        #   그 칸에 딸려 들어간다 — 구운 그림에서 실제로 «몸통 옆에 파란 구슬» 로 나왔다.
        #   → 밴드를 <b>탄 앞에서 끊는다</b>(x1=950). 이러면 칸이 몸통만 품는다.
        Row("RangedLeft",  "body", 445, 525,   R0,   R1, FT, 4,
            folder="RangedAttack", side="Left"),
        Row("RangedRight", "body", 526, 606,   R0,   R1, FT, 4,
            folder="RangedAttack", side="Right"),

        # ⚠ 마지막 칸은 <b>바닥에 깔린 마법진</b>이다 — 땅에 닿아 있어 «발» 로 보이므로
        #   발밑 판정으로도 안 빠진다(`cells_by_feet` 의 마지막 ⚠). 위와 같이 끊는다.
        # ⚠ 마법진은 <b>바닥에 깔려 있어 «발» 로 보인다</b> — 그래서 발밑 판정으로도 안 빠지고
        #   칸이 5개로 잡힌다(`cells_by_feet` 의 마지막 ⚠). ``take`` 로 앞 4칸만 굽는다.
        #   ★ 그래도 ``trim_tail`` 이 필요하다 — 4번 칸의 <b>오른쪽 끝</b>이 마법진 왼쪽
        #     허리에서 끊기게 해 주는 것이 그쪽이다.
        Row("MagicLeft",   "body", 631, 711,   L0,   L1, FT, 5, take=4,
            folder="MagicAttack", side="Left"),
        Row("MagicRight",  "body", 712, 793,   L0,   L1, FT, 5, take=4,
            folder="MagicAttack", side="Right"),

        # ★ 회복 줄은 <b>다섯 칸 모두 몸통</b>이다(빛이 몸에 붙어 있을 뿐이다).
        Row("HealLeft",    "body", 627, 714,   R0,   R1, F, 5, folder="Heal", side="Left"),
        Row("HealRight",   "body", 715, 803,   R0,   R1, HEAL_B, 5, folder="Heal", side="Right"),

        # ⚠ 마지막 칸은 붉은 불덩이(탄) — 원거리와 같은 이유로 밴드를 그 앞에서 끊는다.
        Row("Skill1Left",  "body", 823, 905,   L0,   L1, FT, 4,
            folder="Skill1", side="Left"),
        Row("Skill1Right", "body", 906, 998,   L0,   L1, FT, 4,
            folder="Skill1", side="Right"),

        # ★ 스킬 3(메테오) 시전. 4번째 칸이 <b>정면으로 서서 커지는</b> 그림이라 위로 넘친다 —
        #   밴드를 위로 넉넉히 잡았다(796). 그래서 「스킬 3 모션」 딱지를 꼭 지워야 한다.
        Row("Skill3Left",  "body", 816, 907,  544, 1032, SK3_B, 5, folder="Skill3", side="Left"),
        Row("Skill3Right", "body", 908, 1007, 544, 1032, SK3_B, 5, folder="Skill3", side="Right"),

        # ── 분리 이펙트 (오른쪽 단) ───────────────────────────────────
        Row("Projectile",       "fx",  70, 127, FX0, FX1, C, 4),
        # ★★ 2026-08-21 — ``clusters`` 를 <b>손으로 잰 ``bounds``</b> 로 바꿨다
        #   (유저: *"캐릭터 이펙트도 안 잘리도록"*). 덩어리 판정은 개수(5)는 맞췄지만
        #   경계가 <b>4번 섬광의 오른쪽을 잘라</b> 반토막이 나왔다.
        #   실측(`Tools/sheet_band.py` 2.6배): 섬광 1 1093~1143 · 2 1180~1235 ·
        #   3 1270~1335 · 4 1356~1435 · 5 1466~1481.
        Row("MuzzleFlash",      "fx", 129, 200, FX0, FX1, MUZZLE_B, 5),
        # ★ 시트가 「원거리 / 마법 타격」을 <b>한 줄</b>로 준다 — 같은 그림을 두 칸에 넣는다.
        Row("Impact",           "fx", 227, 325, FX0, FX1, C, 5),
        Row("ImpactMagic",      "fx", 227, 325, FX0, FX1, C, 5),
        # ⚠ 이 두 줄은 <b>덩어리로도 못 센다</b> — 불덩이의 «꼬리» 가 다음 칸까지 뻗어
        #   덩어리가 서로 붙는다(실측: 5개가 4개·2개로). 발이 없어 `feet` 도 못 쓴다.
        #   그래서 경계를 눈으로 재서 박았다.
        Row("Skill1Projectile", "fx", 358, 426, FX0, FX1,
            ("bounds", [1075, 1140, 1195, 1285, 1405, 1534]), 5),
        Row("Skill1Fx",         "fx", 428, 546, FX0, FX1,
            ("bounds", [1075, 1150, 1232, 1318, 1404, 1534]), 5),
        # ★★ 메테오 — 위는 떨어지는 쪽(배선 없음) · 아래는 타는 쪽(지면 연출). 맨 위 ★★ 참조.
        #   ⚠ 위 줄은 «떨어지는 불덩이» 가 서로 이어져 4덩어리로 잡힌다. 배선하지 않는
        #     그림이라 그대로 둔다(억지로 5로 가르면 꼬리가 잘린다).
        Row("Unused_MeteorFall", "fx", 568, 691, FX0, FX1, C, 4),
        Row("Skill3Fx",          "fx", 692, 800, FX0, FX1, C, 4),
        Row("HealFx",            "fx", 832, 897, FX0, FX1, C, 5),
        Row("Unused_HealFxAlt",  "fx", 898, 997, FX0, FX1, C, 4),
    ],
    no_direction=("Projectile", "MuzzleFlash", "Impact", "ImpactMagic",
                  "Skill1Projectile", "Skill1Fx", "Skill3Fx", "HealFx",
                  "Unused_MeteorFall", "Unused_HealFxAlt"),
    scale_reference="IdleLeft",
    # ★ 정규화 안 함 — 맨 위 ⚠ 참조(시트가 「2x / 1x」 견본으로 같은 크기임을 밝힌다).
    scale_metric="min",
    # ★ 옆 칸 조각을 확실히 끊는다 — 프레임이 닿아 있어 기본값(0.12)으로는 붙는다.
    dominant_join=0.05,
)


if __name__ == "__main__":
    run(SPEC)
