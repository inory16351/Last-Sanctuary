# -*- coding: utf-8 -*-
"""세라피엘(9013 · 「침묵의 총성」) 모션 시트 → 프레임 분해 (2026-08-21 · 2026-08-22 전면 재측정).

원본 — **세 장 중 `_02`** 를 쓴다
==================================
``<볼트>/리소스/sprites/asset_Seraphiel_01.png`` (1536x1024 · RGBA)
``<볼트>/리소스/sprites/asset_Seraphiel_02.png`` (1536x1024 · RGBA)  ← ★ 정본
``<볼트>/리소스/sprites/asset_Seraphiel_03.png`` (1536x1024 · **RGB**)

⚠⚠ <b>``_03`` 은 쓸 수 없다</b> — 알파가 <b>체커보드 무늬로 구워져</b> 있다(모드가 RGB 다).
  투명이 «회색 두 색이 번갈아 칠해진 격자» 로 <b>화소에 박혀</b> 있어서, 배경 판정이
  두 색 중 <b>하나만</b> 배경으로 잡고 나머지를 그림으로 본다 — 프레임마다 격자가 남는다.
  ★ ``_02`` 는 RGBA 이고 ``_01`` 보다 크게 그려져 있다 → ``_02`` 가 정본.

★★★ 2026-08-22 — <b>좌표를 통째로 다시 재고 공통 몸통(`char_sheet`)으로 옮겼다</b>
==================================================================================
유저 리포트: *"세라피엘 이동 중 모션 짤리는 문제"*. 실측해 보니 <b>세 가지가 겹쳐</b> 있었다.

<b>① 이동 줄은 10장인데 9장만 굽고 있었다.</b> 실측(딱지를 지운 뒤 · y190~266 열 덩어리):

    x 345~466 · 478~575 · 594~688 · 707~820 · 830~927
    x 943~1037 · 1043~1148 · 1160~1268 · 1286~1374 · 1379~1484      ← <b>열 장</b>

  옛 좌표는 ``x0 = 480`` 이라 <b>첫 장(345~466)을 통째로 버렸고</b>, 남은 아홉 장을
  «폭 ÷ 9»(:func:`skin_sheet.cells_by_span`) 로 갈랐다. 간격이 고르지 않아
  (실측 틈 4~18px) 칸이 조금씩 밀리고, 밀린 칸이 <b>날개를 잘랐다</b>.

<b>② 그 밀림을 «칸을 깎아서» 덮고 있었다.</b> ``CELL_INSET = {"Move": 4}`` 가 그것이다.
  칸을 양쪽에서 4px 씩 깎으면 옆 프레임의 꼬리는 떨어지지만 <b>자기 날개도 같이 잘린다</b> —
  새로 넣은 잘림 검사(:func:`skin_sheet.audit_boxes`)가 이동 9장 중 <b>일곱 장</b>에서
  «오른쪽 18~45px 이 경계를 넘어 이어진다» 고 잡아냈다. 즉 <b>실제로 자르고 있었다</b>.

<b>③ 제목 딱지를 «밴드를 내려서» 피하고 있었다 — 그래서 머리가 잘렸다.</b>
  이동 줄 딱지는 실측 <b>y143~169 · x339~528</b> 이고 1·2번 프레임의 x 와 겹친다.
  옛 좌표는 ``y0 = 168``(딱지 아래)이었는데, 그러면 딱지는 피하지만 그림도 위에서
  잘린다. 실측 프레임 상단은 <b>y168~174</b> 이고 딱지 밑변이 <b>y169</b> 라 <b>1px 도
  여유가 없다</b> — 근거리·마법·스킬 줄은 같은 이유로 <b>위쪽 11~47px</b> 을 잃고 있었다.

★ 그래서 세 가지를 바꿨다:

  1. <b>딱지를 지운다</b>(:data:`Spec.pills`) — 좌표를 손으로 적지 않고 «가로로 긴 어두운
     줄 + 얇다» 로 재서 찾는다. 이 시트에서 일곱 개를 찾는다(y7·142·275·411·557·679·827).
     실측 근거: 딱지가 있는 행의 최장 어두운 런은 138~318px, <b>그림만 있는 행은 최대 79px</b>.
  2. <b>밴드를 그림 그대로</b> 잡는다 — 딱지가 없으니 내릴 이유가 없다.
  3. <b>칸을 «발밑» 으로 가른다</b>(:func:`skin_sheet.cells_by_feet`) — 이 시트는 검·총구
     화염·잔상이 옆 칸까지 뻗어 열로는 못 가르는데(근거리 줄은 x252~573 이 한 덩어리다)
     <b>발은 땅에 따로 놓여 있다</b>. 근거리+원거리 한 줄이 정확히 <b>13칸(7+6)</b> 으로 갈린다.
     ⚠ 스킬1 만 예외다 — 4·5번이 <b>반투명 잔상</b>이라 발이 안 잡힌다(4칸으로 나온다).
       그 줄만 실측 경계를 ``bounds`` 로 박았다.

★ ``CELL_INSET`` 은 <b>지웠다</b> — 칸이 맞으면 깎을 이유가 없다.

★★ 공통 몸통으로 옮기면서 <b>덤으로 따라오는 것들</b>이 있다(유저 지시 *"스킨 균일성
   직접 검토"*): 줄마다 «몸통만» 재서 대기와 견주는 검사(`report_body_size`) · 머리·상자
   두 기준으로 <b>크기를 대기에 맞추는 정규화</b>(`measure_scale`) · 보는 방향 실측
   (`report_tilt`) · 갇힌 배경 되돌리기 · 발밑 그림자 지우기. 옛 스크립트에는 <b>하나도
   없었다</b> — 그래서 새 캐릭터 셋만 «크기가 들쭉날쭉» 했다.

무엇이 어디로 가나
==================
| 시트 줄 | 폴더 | 장수 |
|---|---|---|
| 대기 모션 (Idle) | ``Idle`` | 8 |
| 이동 모션 (Move / Walk) | ``Move`` | **10** — <b>원본이 왼쪽</b> |
| 근거리 공격 (Melee / Gun-Kata) | ``MeleeAttack`` | 7 |
| 원거리 공격 (Ranged Attack) | ``RangedAttack`` | 6 |
| 마법 모션 (Magic Attack) | ``MagicAttack`` | 6 |
| 회복 모션 (Heal Skill) | ``Heal`` | 6 |
| 스킬 1 (Blink / Teleport Back) | ``Skill1`` | 6 — 「회피 기동」(80037) · <b>원본이 왼쪽</b> |
| 스킬 2 (Bullet Storm / Gatling) | ``Skill2`` | 6 — 「종말의 선언」(80039) |

⚠ 「명사수」(80038)는 <b>모션이 없다</b> — 크리티컬 확률이 영구히 오르는 상시 패시브라
  시트에도 그런 줄이 없다. 없는 것을 지어내지 않는다.

이펙트 — ★★ <b>x 구획을 다시 쟀다</b>
=====================================
옛 좌표는 네 상자가 <b>전부 밀려</b> 있었다(실측 대조):

| 상자 | 옛 x | <b>실측 x</b> |
|---|---|---|
| 총알 / 투사체 | 10~292 | **16~429** |
| 총구 이펙트 | 295~566 | **461~653** |
| 근거리 이펙트 | 568~806 | **684~995** |
| 마법 이펙트 | 870~1180 | **1045~1508** |
| 회복 이펙트 | 10~378 | **11~444** |
| 스킬1 이펙트 | 466~768 | **482~886** |
| 스킬2 이펙트 | 1140~1332 | **937~1518** |

즉 총구 이펙트 칸에는 <b>총알 상자의 오른쪽 끝과 총구의 왼쪽 절반</b>이 섞여 구워지고
있었다. 유저가 본 «이펙트가 짤린» 것의 정체다.

★★ <b>한 상자 안에 세로로 2~3 줄</b>이 쌓여 있고 그 줄들은 «단계» 가 아니라 <b>변형</b>이다
  (총알 상자는 같은 배열이 3줄 = 탄종 3가지). 그래서 <b>한 줄만</b> 굽는다. 어느 줄인지는
  세로 잉크 프로파일의 골로 갈랐다(실측):

    총알·총구  y709~755 / 756~801 / 802~830   → <b>맨 위</b>
    근거리     y705~766 / 767~830             → <b>맨 위</b>
    마법       y704~763 / 764~820             → <b>맨 위</b>
    회복       y863~908 / <b>909~1010</b>     → <b>아래</b>(진행 단계가 그려진 줄)
    스킬1      y863~928 / <b>929~1016</b>     → <b>아래</b>
    스킬2      y863~912 / <b>913~1023</b>     → <b>아래</b>

⚠ 이펙트 상자는 글로우가 서로 닿아 <b>열로 못 가른다</b>(실측: 회복 상자 전체가
  x11~444 한 덩어리다). 그래서 이펙트 줄만 «폭 ÷ 장수»(``span``)를 쓴다 — 상자의
  x 를 정확히 잡아 놓으면 그 안에서는 간격이 고르다.

사용법:  python Tools/seraphiel_skin_build.py
다음:    유니티 메뉴 **LastSanctuary/스킨/원화 폴더로 스킨 에셋 만들기**
        그 다음 `python Tools/measure_skin_tiles.py`
"""

import os

from vault_path import VAULT, PROJECT
from char_sheet import Row, Spec, run

SRC = os.path.join(VAULT, "리소스", "sprites", "asset_Seraphiel_02.png")

DST_ROOT = os.path.join(PROJECT, "Assets", "_Project", "Art", "Char_Asset",
                        "Char_Asset_Seraphiel", "Char")

#: 스킬1 줄의 칸 경계 — 4·5번이 <b>반투명 잔상</b>이라 발밑 판정이 안 듣는다(맨 위 ⚠).
#: 실측한 열 덩어리: 19~115 · 127~218 · 233~321 · 342~431 · 483~604 · 616~741.
SKILL1_BOUNDS = [19, 127, 233, 342, 483, 616, 742]

ROWS = [
    # ── 몸통 ── 칸은 <b>발밑</b>으로 가른다(맨 위 ★ 3).
    Row("Idle",         "body",  36, 128,  430, 1535, ("feet",),  8),
    Row("Move",         "body", 166, 268,  330, 1535, ("feet",), 10, side=None),
    Row("MeleeAttack",  "body", 286, 400,    0,  791, ("feet",),  7),
    Row("RangedAttack", "body", 300, 400,  792, 1535, ("feet",),  6),
    Row("MagicAttack",  "body", 410, 560,    0,  761, ("feet",),  6),
    Row("Heal",         "body", 412, 560,  762, 1535, ("feet",),  6),
    Row("Skill1",       "body", 583, 677,    0,  755, ("bounds", SKILL1_BOUNDS), 6),
    Row("Skill2",       "body", 583, 677,  756, 1535, ("feet",),  6),

    # ── 이펙트 ──
    #
    # ★★ <b>«폭 ÷ 장수» 를 버리고 실측 경계(``bounds``)로 바꿨다</b> (2026-08-22).
    #   이 팔레트는 부품 간격이 <b>고르지 않다</b>(총알 상자 실측 간격 30·70·45·32·30·32·30).
    #   ``span`` 은 «고르다» 를 전제하므로 칸이 밀리고, 밀린 칸이 옆 부품을 반씩 물어 온다.
    #
    # ★ 아래 경계는 전부 <b>덩어리를 재서</b> 나온 값이고, 눈으로 대조했다.
    Row("Projectile",    "fx", 709,  755, 16,  169,
        # ★ 총알 상자에는 <b>같은 탄이 작아지는 여덟 칸</b>이 있다 — «날아가는 애니메이션» 이
        #   아니라 <b>거리감 견본</b>이다. 그대로 구우면 탄환이 <b>비행 중에 사라진다</b>.
        #   그래서 «탄 + 화염 꼬리» 가 또렷한 <b>앞 세 칸만</b> 쓴다.
        ("bounds", [16, 51, 123, 170]), 3),
    Row("MuzzleFlash",   "fx", 709,  755, 346,  653,
        # ⚠ 옛 x(295~566)는 <b>총알 상자의 오른쪽 끝</b>을 물고 <b>총구 4번째</b>를 잘랐다.
        ("bounds", [346, 445, 538, 603, 654]), 4),
    Row("MeleeTravelFx", "fx", 705,  766, 682,  995,
        # ★ 네 덩어리 중 <b>2번은 작은 별</b>이다 — 참격 사이에 별이 한 장 끼면 튄다.
        #   그래서 <b>초승달 셋만</b> 남긴다(``keep``).
        ("bounds", [682, 805, 848, 918, 996]), 4, keep=[0, 2, 3]),
    Row("ImpactMagic",   "fx", 711,  763, 1045, 1290,
        # ★ 이 줄의 뒤쪽 둘(혜성·잔반짝임)은 <b>착탄이 아니다</b> — 혜성은 날아가는 탄이고
        #   반짝임은 소멸이다. 고리→별→별→고리 <b>네 칸</b>이 터지는 진행이다.
        ("bounds", [1045, 1112, 1170, 1228, 1291]), 4),
    Row("HealFx",        "fx", 863, 1010, 11,  444,
        # ★★ 옛 밴드(909~1010)는 <b>위를 잘랐다</b> — 이 상자는 «두 줄» 이 아니라
        #   <b>바닥 고리 + 그 위 반짝임</b>이 한 벌인 <b>네 단계</b>다(눈으로 확인).
        #   칸은 <b>바닥 고리 띠</b>(y960~1000)에서 재면 깨끗하게 넷으로 갈린다.
        ("bounds", [11, 94, 182, 328, 445]), 4),
    Row("Skill1Fx",      "fx", 929, 1016, 483,  879,
        ("bounds", [483, 687, 775, 880]), 3),

    # ★★ <b>스킬2 이펙트는 «세로로 세 줄» 이 곧 세 장이다</b> — 같은 화염이 세 세기로
    #   그려져 있다(갯틀링의 총구 폭발). x 는 같고 y 만 다르므로 <b>줄을 셋으로 나눠
    #   한 폴더에 이어 굽는다</b>(``folder`` + ``start``).
    #   ⚠ 하나의 밴드로 묶으면 <b>한 프레임에 화염 두 줄이 겹쳐</b> 구워진다(실제로 그랬다).
    #   실측 골: y915(잉크 6) · y963. 오른쪽의 연기·십자는 아래 ``Unused_`` 로 남긴다.
    Row("Skill2FxA", "fx", 857,  915, 937, 1168, ("span",), 1, folder="Skill2Fx", start=0),
    Row("Skill2FxB", "fx", 916,  963, 937, 1168, ("span",), 1, folder="Skill2Fx", start=1),
    Row("Skill2FxC", "fx", 964, 1022, 937, 1168, ("span",), 1, folder="Skill2Fx", start=2),

    # ══════════════════════════════════════════════════════════════════
    # ★★ 2026-08-24 — <b>«울타리» 줄</b>. 굽기도 하지만 진짜 목적은 <b>이웃을 막는 것</b>이다.
    #
    # <b>무엇이 잘못돼 있었나</b> — 총구 섬광·마법 착탄 프레임의 <b>아래쪽에 다음 줄의
    # 그림이 반쯤 구워져</b> 있었다(구운 PNG 로 확인 · `check_frame_edges` 가 «아래 35~61%»
    # 로 잡았다). 원인은 `char_sheet.grow_limits`(UI-49 신설)다:
    #
    #   그 함수는 «<b>x 가 겹치는 이웃 줄</b>» 의 밴드까지만 넓힌다. 그런데 이 팔레트는
    #   한 상자에 <b>같은 부품이 세기별로 2~3줄</b> 쌓여 있고(총구 3줄 · 착탄 2줄),
    #   <b>그 아랫줄들이 ROWS 에 선언돼 있지 않았다.</b> 이웃이 «없으니» 상한이
    #   `GROW_MARGIN`(26px)까지 열렸고, 글로우가 이어진 아랫줄을 그만큼 끌고 왔다.
    #
    #   실측(알파>60 · 열 x346~653):  라벨 681~707 · <b>1줄 710~753</b> · 2줄 758~801 · 3줄 804~831
    #   실측(알파>60 · 열 x1045~1290): 라벨 683~709 · <b>1줄 711~760</b> · 2줄 768~816
    #
    # → 아랫줄을 <b>선언만 해두면</b> 상한이 두 밴드의 중간으로 내려와 침범이 끝난다.
    #   («굽지 않기» 가 아니라 «선언하기» 가 고치는 방법이다 — 좌표를 손으로 조이면
    #    원화가 바뀔 때 또 어긋난다.)
    # ★ 이 줄들은 <b>스킨에 배선하지 않는다</b>(`Unused_` 규약) — 총구 섬광은 평타마다
    #   도는 애니메이션이라 장수를 늘리면 «한 발 쏘는데 12프레임» 이 된다.
    Row("Unused_MuzzleFlashB", "fx", 758, 801, 346, 653, ("span",), 1),
    Row("Unused_MuzzleFlashC", "fx", 804, 831, 346, 653, ("span",), 1),
    Row("Unused_ImpactMagicB", "fx", 768, 816, 1045, 1290, ("span",), 1),

    # 원화 보존 — 배선하지 않는다(대응하는 스킬 칸이 없다).
    Row("Unused_Skill2Smoke", "fx", 905, 1015, 1185, 1430, ("span",), 1),
    Row("Unused_Skill2Cross", "fx", 858, 1010, 1432, 1520, ("span",), 1),
]

#: 좌우 방향이 없는 묶음.
NO_DIRECTION = {"Projectile", "MuzzleFlash", "MeleeTravelFx", "ImpactMagic",
                "HealFx", "Skill1Fx", "Skill2Fx",
                "Skill2FxA", "Skill2FxB", "Skill2FxC",
                "Unused_MuzzleFlashB", "Unused_MuzzleFlashC", "Unused_ImpactMagicB",
                "Unused_Skill2Smoke", "Unused_Skill2Cross"}

#: ★★ <b>원본이 왼쪽을 보고 있는 줄</b> — 뒤집어서 Right 로 굽는다.
#:
#: ⚠⚠ <b>2026-08-21 수정</b> — 처음에 «원본이 전부 오른쪽» 으로 잡았는데 <b>틀렸다</b>.
#:   유저 리포트: *"왼쪽을 보고있는데 오른쪽으로 이동중"*. 다시 재보니 줄마다 다르다(실측):
#:
#:     <b>이동</b>    가면이 <b>왼쪽</b> · 총구가 왼쪽 아래 · <b>날개가 오른쪽으로 끌린다</b>  → 왼쪽
#:     <b>스킬1</b>   가면이 <b>왼쪽</b> · 총이 왼쪽 아래 · 잔상이 오른쪽                      → 왼쪽
#:     원거리·마법·스킬2  총구가 <b>오른쪽</b>으로 뻗고 <b>총구 화염도 오른쪽</b>            → 오른쪽
#:     근접(건카타)  회전 동작이라 프레임마다 갈리는데 3번의 총구가 오른쪽 위다              → 오른쪽
#:     대기·회복     정면 대칭(총을 아래로 모으고 있다)                                      → 그대로
#:
#: ★ <b>모든 «Right» 칸이 오른쪽을 보게 맞춘다</b> — 한 줄이라도 어긋나면 걷다가 공격할 때
#:   <b>캐릭터가 좌우로 뒤집힌다</b>(`CharacterAnimator` 는 `FacingRight` 하나로 두 칸을 고른다).
#: ★ 판단 재료는 `char_sheet.report_tilt` 가 줄마다 찍어 주는 «머리−발» 값이다.
ORIGINAL_SIDE = {"Move": "Left", "Skill1": "Left"}

SKIN_SPEC = {
    "skinAssetName": "Skin_Seraphiel",
    "outputFolder": "Assets/_Project/Resources/Skins",
    "displayName": "세라피엘",
    "framesPerSecond": "10",
    # 갯틀링을 쏘는 인물이라 공격 프레임을 빠르게 돌린다.
    "attackFramesPerSecond": "16",
    # 총알 — 화면에서 «탄환» 으로 읽히는 최소 크기.
    "projectileWidthTiles": "0.6",
    # ★★ <b>이펙트 크기를 «타일» 로 못박는다</b> (2026-08-22 · 유저 지시 *"확실하게
    #   이펙트가 표현되도록"*). 비워 두면(<b>0</b>) 배율이 1 이 되어 <b>구워진 픽셀 크기가
    #   그대로 화면 크기</b>가 된다 — 그러면 원화를 다시 자를 때마다(이번처럼) 연출이
    #   커졌다 작아졌다 한다. 이 프로젝트의 규칙은 «몇 타일로 그릴지만 적고 배율은 코드가
    #   계산한다»(`CombatProjectileFx.ScaleForWidthTiles`)이고, 그 규칙을 지금 적용한다.
    # ★ 값의 근거 — 몸통이 1.4~1.8 타일이다(`contentSizeTiles`). 참격은 몸보다 조금 커야
    #   «베었다» 로 읽히고, 착탄은 몸 정도면 «맞았다» 로 읽힌다. 카이론·아루의
    #   `impactWidthTiles`(1.4~1.6)와 같은 대역이다.
    "meleeTravelWidthTiles": "1.6",
    "impactWidthTiles": "1.4",
}

SPEC = Spec(
    title="세라피엘",
    sources={"01": SRC},
    dst_root=DST_ROOT,
    skin_spec=SKIN_SPEC,
    rows=ROWS,
    # ★ 딱지를 재서 지운다 (맨 위 ★ 1). 좌표를 손으로 적지 않는다.
    pills={},
    no_direction=NO_DIRECTION,
    original_side=ORIGINAL_SIDE,
    default_side="Right",
    scale_reference="Idle",
    # ★★ <b>크기 정규화는 끈다</b> (2026-08-22 · 재보고 내린 결정).
    #
    # 이 인물은 <b>날개가 하얗다</b> — `skin_sheet.head_pixels` 는 «상단 창의 밝고 저채도인
    # 픽셀» 을 머리로 세므로 <b>펼친 날개가 머리로 잡힌다</b>. 이동 줄은 날개를 뒤로 크게
    # 펼치므로 «머리가 커졌다» 가 되어 배율이 <b>x0.713</b>(29% 축소)로 나왔다 —
    # 걸으면 캐릭터가 작아지는, <b>고치려던 것보다 나쁜</b> 결과다.
    # 아르세니아가 같은 함정을 밟았다고 `char_sheet.measure_scale` 주석이 적어 두었다.
    #
    # ★ 그리고 <b>이 시트는 정규화가 필요 없다</b> — `report_body_size` 로 «몸통만» 재면
    #   가로가 <b>83~84px 로 여덟 줄이 같다</b>(이펙트가 두꺼운 마법·스킬2·회복만 크게 나오는데
    #   그건 총구 화염이 «두꺼운 열» 판정에 들어간 것이다). 원화가 이미 균일하다.
    # ⚠ 그래도 <b>재서 찍기는 한다</b> — 원화가 바뀌면 그 표가 먼저 알려준다.
    scale_metric=None,
    # ⚠ 0.06(기본) 은 «본체에서 7px 떨어진 덩어리까지 이어 붙인다» 는 뜻이라 이 시트에서는
    #   <b>남의 날개까지</b> 붙인다. 0.02 는 하한 4px 로 떨어져 자기 것만 남는다.
    dominant_join=0.02,
)


def main():
    run(SPEC)


if __name__ == "__main__":
    main()
