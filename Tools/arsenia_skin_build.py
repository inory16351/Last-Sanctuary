# -*- coding: utf-8 -*-
"""아르세니아(캐릭터 9010) 모션 시트 → 프레임 분해.

원본: ``<볼트>/리소스/sprites/Arsenia_asset.png`` (1536x1024)

★★ 2026-08-20 — **시트가 교체되어 전부 다시 쟀다**
--------------------------------------------------
유저 지시: *"아르세니아·카이론 아루/불칸 새로 넣은 에셋으로 스킨 재제작 … 이펙트가
짤리거나 캐릭터의 크기/피벗이 어긋나지 않도록"*.

이 시트는 <b>세 번째 판본</b>이다(RGB → 알파 PNG → 이번 것). 120-11절이 «배치가 바뀌어
좌표 재측정이 남았다» 고 적어 둔 그 일을 여기서 끝낸다.

★ **이 시트는 앞의 둘보다 훨씬 다루기 쉽다** — 이유가 세 가지다:

1. <b>구획이 한 줄에 하나</b>다(불칸·카이론은 한 줄에 둘~셋이었다). 그래서 밴드가 곧 구획이다.
2. <b>제목 딱지가 전부 왼쪽 끝</b>(x 10~250)에 모여 있다. 프레임은 x 145 부터 시작하므로
   <b>지울 필요가 없다</b> — 밴드 x0 를 프레임 시작에 맞추면 딱지가 저절로 빠진다.
3. <b>이펙트마다 딱지가 따로</b> 붙어 있다(「투척 폭발」·「영역 장판 (지속)」·「하늘 레이저」…).
   카이론에서 «어느 이펙트가 어느 스킬 것인지 알 수 없어» ``Unused_`` 로 남긴 문제가
   여기서는 없다 — <b>시트가 스스로 말해 준다.</b>

★★ **탈진 — 시트가 「스킬3 후」라고 적어 두었다**
------------------------------------------------
유저 지시: *"아르세니아 탈진은 <b>3스킬로 탈진 했을때만</b> 넣음 보스의 스킬에 기절
당했을땐 나오지 않음"*.

시트의 딱지가 <b>「탈진 모션 (스킬3 후)」</b> 이다 — 원화가 이미 그 규칙으로 그려져 있다.
코드도 그렇게 되어 있다: :meth:`CharacterAnimator.PlayStunMotion` 을 부르는 곳은
<b>단 한 곳</b>이고(``CharacterPassives.Newcomers`` 의 「완성되지 못한 고귀함」), 보스 스킬은
:meth:`UnitCombat.ApplyBind` 만 부른다. 즉 <b>구조상 보스 기절로는 이 모션이 나올 수 없다.</b>
⚠ 나중에 다른 곳에서 ``PlayStunMotion`` 을 부르게 되면 이 규칙이 깨진다 — 그때 이 주석을 볼 것.

⚠ **사망 모션을 굽지 않는다** (유저 지시: *"사망 모션 넣지 않기 … 단, 골렘은 예외"*).
   이 시트에는 사망 줄이 없다. 「탈진」은 사망이 아니다(엎드린 자세로 살아 있다).
⚠ **「캐릭터 기본 이미지」 구획(x 750~1360 · y 905~1010)은 굽지 않는다** — 원화 참조용
   전신샷과 소지품 그림이다. 밴드가 그쪽으로 넘어가지 않게 탈진 줄의 x1 을 560 으로 막았다.
⚠ (옛 판단) 크기 정규화를 하지 않았다 — 아래 ★★ 5 에서 <b>틀린 것으로 판명</b>되어 켰다.

★★ 2026-08-21 — **이동 모션이 통째로 잘려 나오던 것을 고쳤다**
--------------------------------------------------------------
유저 리포트: *"아르세니아 … 지금 이동 시마다 모션이 다 잘려서 나옴"*. 실제로 그랬다 —
구운 그림에서 <b>9장 중 3장이 «날개 조각» 이나 «반쪽 몸통»</b>이었다.

원인이 셋이고, 셋 다 <b>개수 검사(``expect``)를 통과하는</b> 종류의 오류다:

1. ★★ **이동 줄의 칸 가르기가 ``feet`` 였다.** 발밑만 보는 판정인데 아르세니아는
   <b>망토 자락이 바닥을 따라 이어져</b> 발밑 띠에서 조각이 붙었다 — 그러면
   :func:`skin_sheet.merge_to_count` 가 장수를 맞추려고 <b>엉뚱한 곳을 합친다</b>.
   실측(왼쪽 줄): ``(546,687)`` 한 칸에 <b>몸통 둘</b>이 들어가고 다음 칸 ``(688,730)`` 은
   <b>43px 짜리 날개 조각</b>이었다. 오른쪽 줄은 ``(334,541)`` 이 <b>208px</b>(몸통 셋)이었다.
   → 이동 줄은 <b>빈 열이 뚜렷하다</b>(8곳 전부 3~20px). 그래서 손으로 잰 ``bounds`` 로 박았다.
   ⚠ 아루·카이론에서 «자동 세 가지가 다 안 된다» 던 그 결론이 여기서도 같다.

2. **오른쪽 이동 줄의 밴드가 발을 깎고 있었다** — 그림은 ``y221~319`` 인데 밴드가 ``~316``
   이라 <b>아래 3줄(46·48·48 px = 신발 바닥)</b>이 잘렸다. 개수 검사는 이것을 못 본다.

3. ★★ **「스킬 3」은 6장인데 ``expect`` 가 7 이었다.** 이 문서의 표에도 <b>6</b> 이라고
   적혀 있는데 :data:`Row` 에만 7 이 들어가 있었고, ``clusters`` 가 <b>마침 7</b>을 내놓아
   (3번 프레임의 후광이 끊겨 두 덩어리로 잡혔다) 검사를 통과했다. 결과가 «반쪽 + 날개만» 이다.
   → 장수를 6 으로 바로잡고 ``bounds`` 로 박았다. 밴드 아래끝도 ``846`` → ``842`` 로 올렸다
   (``844~847`` 이 「캐릭터 기본 이미지」 판의 <b>금색 테두리</b>다 — 구운 그림에 선이 박혔다).

★ 이 일 때문에 도구를 <b>둘</b> 만들었다:
  · `Tools/sheet_split.py` — «빈 열 + 국소 최소» 로 <b>경계 자체</b>를 찍는다.
    `sheet_rows.py` 는 «개수가 맞는가» 만 보므로 위 3번 같은 오류를 <b>구조적으로 못 잡는다.</b>
  · `Tools/sheet_clip.py` — <b>칸마다</b> 위·아래로 그림이 이어지는지 본다.
    `char_sheet.warn_if_clipped` 은 <b>줄 단위</b>라 20줄 중 19줄에 경고가 떠서
    «정말 잘린 한 장» 이 묻힌다(아래 4번이 그렇게 묻혀 있었다).

★★ 4. **「스킬 2」 3번째 프레임의 모자 꼭대기가 잘려 있었다**
    유저 리포트: *"아르세니아 중간에 머리 잘린 거 하나 들어가 있고"*.
    그 프레임만 몸을 세워 <b>모자가 15px 더 높다</b>(모자 위의 붉은 구슬 장식이 y588~607).
    밴드가 ``y608`` 부터라 <b>구슬이 통째로 잘리고 모자가 평평하게</b> 나왔다
    (`sheet_clip.py` 가 「윗변 연속 36px」로 잡아 준다).
    → 스킬 2 밴드를 ``y593`` 부터로 올렸다. 실측으로 그 위 15줄에 남는 것은
    <b>다른 프레임의 같은 장식들</b>(x273~288 · 399~428 · 762~769 · 834~850)뿐이다.
    ⚠ ``588~592`` 까지 더 올릴 수는 없다 — 그 다섯 줄에는 <b>위 회복 줄의 신발</b>이
      같은 x 에 겹쳐 있다. 구슬 꼭대기 5px 을 포기하고 신발이 안 들어오는 쪽을 골랐다.
    ⚠ 회복 줄의 아래끝도 ``592`` → ``591`` 로 내렸다 — y592 의 잉크는 회복의 것이 아니라
      <b>스킬 2 의 구슬</b>이다(그대로 두면 회복 프레임 밑에 붉은 점이 박힌다).
    ⚠ 「스킬 2」 딱지 지움도 ``y593`` 부터로 올렸다(밴드가 딱지 판을 지나가게 됐다).

★★ 5. **이동할 때 작아지던 것 — 크기 정규화를 켰다**(``scale_metric="head"``)
    유저 리포트: *"어떤 캐릭터든 간에 이동할때 작아지는 문제 수정해 아르세니아도
    이동할때 작아질거 같은데"*. <b>맞았다.</b> 실측(대기 기준):

    | 줄 | 상자 높이 | 머리 면적 | 판정 |
    |---|---:|---:|---|
    | 이동   | x1.161 | x1.132 | <b>둘이 일치 → 정말 작다</b> |
    | 원거리 | x1.312 | x1.319 | 일치 |
    | 회복   | x1.232 | x1.238 | 일치 |
    | 마법   | x1.188 | x1.371 | 어긋난다(머리가 가려진 프레임이 있다) |
    | 스킬 2 | x1.278 | x1.098 | 어긋난다(웅크린 자세) |
    | 스킬 3 | x0.737 | x0.699 | 일치 — <b>원래 크다</b>(천사 강림) |

    ★ 두 기준이 <b>일치하는 줄이 다수</b>라 시트가 정말 줄마다 다른 크기로 그려진 것이다
      (아루와 같은 사고). 기준은 <b>머리 면적</b>을 쓴다 — 상자 높이는 «웅크린 자세» 를
      크기 오류로 잘못 읽는다(`skin_sheet.head_pixels` 의 긴 주석).
    ⚠⚠ **「스킬 3」은 정규화 기준에서 뺐다**(``scale=False``). 그 줄은 <b>천사로 변신하는
      연출</b>이라 원화가 일부러 40% 크다 — 대기에 맞춰 줄이면 연출이 죽는다.
      「탈진」이 이미 같은 이유로 빠져 있다(누운 자세).

⚠ 덤으로 고친 두 곳(둘 다 «밴드가 그림을 깎던» 것):
  · 대기 밴드 ``y18`` → ``16`` — 아홉 프레임 <b>모자 꼭대기 2px</b> 가 잘려 있었다.
  · 원거리 밴드 ``y404`` → ``405`` — 아래 1줄(39px)이 잘려 있었다.

무엇이 어디로 가나
------------------
| 폴더 | 장수 | 스킨 칸 | 근거 |
|---|---:|---|---|
| ``Idle`` | 9 | ``idleRight/Left`` | 한 방향 + 미러 |
| ``Walk`` | 9+9 | ``walkRight/Left`` | <b>좌·우 원화가 따로</b> 있다(딱지가 「이동 모션 (좌)」·「(우)」) |
| ``RangedAttack`` | 7 | ``rangedRight/Left`` | 「원거리 공격 (포션 투척)」 — <b>붉은</b> 물약 |
| ``MagicAttack`` | 7 | ``magicRight/Left`` | 「마법 (강화 포션 투척)」 — <b>보라</b> 물약 |
| ``Heal`` | 7 | ``healRight/Left`` | 「회복 (힐 포션 투척)」 — <b>초록</b> 물약 |
| ``Skill2`` | 7 | ``skill2Right/Left`` | **성스러운 축복**(80029) 「연금 영역」 |
| ``Skill3`` | 6 | ``skill3Right/Left`` | **완성되지 못한 고귀함**(80030) 「천사 강림」 |
| ``Stun`` | 4 | ``stunRight/Left`` | 「탈진 모션 (스킬3 후)」 |
| ``Projectile`` | 7 | ``projectileFrames`` | 붉은 물약병 — 원거리 |
| ``Impact`` · ``ImpactMagic`` | 5 · 5 | ``impactFrames`` · ``magicImpactFrames`` | 딱지가 「일반 폭발 이펙트 (<b>원거리/마법</b>)」 — 한 그림을 두 칸에 |
| ``HealFx`` | 5 | ``healFxFrames`` | 「회복 이펙트」(초록) |
| ``Skill2Fx`` | 4 | ``skill2Fx`` | 「영역 장판 (<b>지속</b>)」 — 지면 연출이므로 이쪽이 맞다 |
| ``Skill3Fx`` | 5 | ``skill3Fx`` | 「하늘 레이저」 |
| ``Unused_PotionPurple`` / ``Green`` | 7 · 7 | — | 마법·회복용 물약병. 스킨에 투사체 칸이 <b>하나뿐</b>이라 남긴다 |
| ``Unused_Skill2Burst`` | 4 | — | 「투척 폭발」 — 장판과 둘 다 배선할 칸이 없다 |
| ``Unused_Skill3Angel`` | 4 | — | 「천사 강림 연출」 — 몸통 줄이 이미 그 연출을 담고 있다 |
| ``Unused_Skill3Blast`` | 4 | — | 「폭발/섬멸」 |

⚠ ``Projectile`` 을 <b>붉은 물약</b>으로 고른 이유 — 스킨에 투사체 칸이 하나뿐이고,
  아르세니아의 <b>기본 공격 유형이 원거리</b>다(「불안정성」이 근거리를 금지한다).
  가장 자주 보이는 것을 배선하는 것이 맞다.

사용법:  py -3 Tools/arsenia_skin_build.py
"""

import os

from vault_path import VAULT, PROJECT
from char_sheet import Row, Spec, run

SRC = os.path.join(VAULT, "리소스", "sprites", "Arsenia_asset.png")
DST = os.path.join(PROJECT, "Assets", "_Project", "Art", "Char_Asset",
                   "Char_Asset_Arsenia", "Char")

F = ("feet",)         # 몸통 — 발밑(다리가 갈라지면 expect 까지 합쳐진다)
C = ("clusters",)     # 이펙트 — 덩어리

#: 몸통 줄의 x 범위. 왼쪽은 <b>제목 딱지 다음</b>(145), 오른쪽은 시트 여백까지.
B0, B1 = 145, 1045
#: 오른쪽 이펙트 단. 이펙트마다 붙은 <b>작은 딱지</b>(x 1080~1150) 다음부터다.
FX0, FX1 = 1155, 1521
#: 물약병 줄만 딱지가 위에 있어 x 를 더 넓게 쓴다.
PT0, PT1 = 1080, 1521

#: ⚠⚠ **이펙트 단은 덩어리·빈 열로 못 가른다** — 각 묶음을 감싼 <b>둥근 판 테두리</b>가
#:   이펙트로 잡혀 <b>한 줄이 통째로 한 덩어리</b>가 된다(실측: 전부 «1개»).
#:   테두리를 지우려면 판이 여덟 개라 좌표가 여덟 벌 필요하고, 그 선은 이펙트와 색이
#:   비슷해 «지우다 이펙트를 깎을» 위험이 있다.
#:   → 히스톤과 같은 결론: <b>경계를 눈으로 재서 박는다</b>. `Tools/sheet_band.py` 로
#:     2배 확대해 읽은 값이고, 물약병 세 줄은 <b>같은 격자</b>라 한 값을 셋에 쓴다.
POTION_B = ("bounds", [1080, 1118, 1175, 1264, 1332, 1398, 1468, 1522])
IMPACT_B = ("bounds", [1085, 1156, 1243, 1314, 1423, 1522])
HEALFX_B = ("bounds", [1085, 1157, 1243, 1315, 1402, 1512])
SK2BURST_B = ("bounds", [1155, 1222, 1296, 1427, 1522])
SK2FX_B = ("bounds", [1155, 1239, 1325, 1416, 1508])
SK3ANGEL_B = ("bounds", [1155, 1204, 1268, 1345, 1522])
SK3FX_B = ("bounds", [1155, 1206, 1261, 1325, 1402, 1512])
SK3BLAST_B = ("bounds", [1155, 1241, 1336, 1413, 1522])

# ──────────────────────────────────────────────────────────────────────────
#   ★★ 몸통 줄의 손으로 잰 칸 경계 (2026-08-21 · 맨 위 「이동 모션이 통째로 잘려」).
#
#   값은 «빈 열 구간의 가운데» 다(`Tools/sheet_split.py` 가 근거까지 찍어 준다).
#   ⚠ 좌·우 이동은 <b>같은 격자가 아니다</b> — 원화가 줄마다 조금씩 밀려 있어
#     한쪽 값을 다른 쪽에 쓰면 몸통이 반토막 난다. 그래서 따로 적었다.
# ──────────────────────────────────────────────────────────────────────────
WALK_L_B = ("bounds", [162, 248, 345, 447, 550, 641, 735, 836, 942, 1041])
WALK_R_B = ("bounds", [145, 233, 334, 440, 537, 640, 741, 837, 942, 1040])

#: ★ 「스킬 3」 <b>6장</b>. 뒤 넷은 후광이 이어져 빈 열이 없다 — 그 사이의
#:   <b>국소 최소</b>(잉크 64 · 8 · 22 px)를 경계로 삼았다.
SKILL3_B = ("bounds", [55, 183, 339, 552, 705, 846, 1046])

SPEC = Spec(
    title="아르세니아",
    sources={"01": SRC},
    dst_root=DST,
    skin_spec={
        "skinAssetName": "Skin_Arsenia",
        "displayName": "아르세니아",
        "framesPerSecond": "10",
        "attackFramesPerSecond": "14",
        "groundImpactOnly": "0",
        "projectileWidthTiles": "0.8",
        "impactWidthTiles": "1.6",
    },
    # ★★ 제목 딱지를 <b>전부</b> 지운다.
    #
    #   ⚠⚠ 처음에 «딱지는 다 x 250 왼쪽이고 밴드가 145 부터라 저절로 빠진다» 고 판단해
    #     하나만 지웠는데, <b>틀렸다</b> — 원거리 줄의 첫 프레임에 <b>「선 투척)」 글자가
    #     그대로 박혀</b> 나왔다(구운 그림에서 눈으로 확인). 딱지의 길이가 줄마다 다르고
    #     <b>긴 딱지는 x 200 까지</b> 간다(「원거리 공격 (포션 투척)」·「마법 (강화 포션 투척)」).
    #
    #   ★ 그래서 <b>줄마다 오른쪽 끝을 따로</b> 쟀다 — 딱지 다음 첫 그림 구간이 어디서
    #     시작하는지까지 함께 봤다(대기는 딱지 148 · 그림 166 처럼 여유가 있다).
    #   ★ 이펙트 단의 작은 딱지는 x 1150 왼쪽이라 밴드(1155)가 이미 피한다.
    erase=(
        (  18,   44,    4,  155),   # 대기 모션
        ( 127,  153,    4,  165),   # 이동 모션 (좌)
        ( 224,  244,    4,  155),   # 이동 모션 (우)
        ( 314,  347,    4,  207),   # 원거리 공격 (포션 투척) ⚠ y314 부터 — 위 이동(우) 줄로 넘어온 판 모서리
        ( 406,  436,    4,  196),   # 마법 (강화 포션 투척)
        ( 507,  527,    4,  184),   # 회복 (힐 포션 투척)
        ( 593,  631,    4,  176),   # 스킬 2 (연금 영역) ⚠ y593 부터 — 밴드를 올려 딱지 판을 지난다
        # ⚠ 이 딱지만 <b>넉넉히</b> 지운다 — 금색 테두리와 그림자가 상하로 더 뻗어 있어
        #   703~729 만 지우면 <b>얇은 금색 선 두 줄</b>이 남아 그것이 «1번 프레임» 으로
        #   잡혔다(구운 그림에서 실제로 그랬다).
        ( 694,  742,    4,  190),   # 스킬 3 (천사 강림)
        ( 865,  891,    4,  190),   # 탈진 모션 (스킬3 후)

        # ★★ <b>이펙트 묶음을 감싼 「둥근 판」 테두리</b>도 지운다 (2026-08-20).
        #
        #   ⚠ 안 지우면 <b>그 선이 이펙트 프레임에 박힌다</b> — 구운 그림에서 회복·폭발·
        #     장판 프레임 밑에 <b>얇은 회색 선과 ⌐ 모양 꺾쇠</b>가 그대로 나왔다.
        #     선이 이펙트와 <b>같은 밴드 안</b>에 있어 밴드로는 피할 수 없다.
        #
        #   ★ 좌표는 «폭의 80% 이상을 채우는 2~3px 짜리 줄» 을 세어 얻었다(실측) —
        #     이펙트는 그렇게 가로로 꽉 차면서 얇을 수가 없으므로 <b>선만 골라진다</b>.
        #   ⚠ 세로 테두리 두 줄(x 1065·1525)은 <b>시트 높이 전체</b>에 걸쳐 있다.
        (   0, 1023, 1063, 1068),   # 판 왼쪽 테두리
        (   0, 1023, 1522, 1529),   # 판 오른쪽 테두리
        (   9,   13, 1060, 1531),
        ( 199,  203, 1060, 1531),
        ( 211,  215, 1060, 1531),
        ( 307,  311, 1060, 1531),
        ( 318,  322, 1060, 1531),
        ( 422,  426, 1060, 1531),
        ( 434,  438, 1060, 1531),
        ( 528,  532, 1060, 1531),

        # ★★★ <b>이펙트 열의 제목 배너</b>도 지운다 (2026-08-24 · 유저 리포트:
        #   *"아르세니아 투사체에 텍스트 붙어있는거 수정해"*).
        #
        #   ⚠⚠ 위 «이펙트 단의 작은 딱지는 x 1150 왼쪽이라 밴드(1155)가 이미 피한다» 는
        #     <b>틀렸다.</b> 두 가지 이유로 틀렸다:
        #     ① 폭발·회복·투척폭발 세 줄의 배너는 <b>x 1369~1479 까지</b> 길다
        #        (「일반 폭발 이펙트 (원거리/마법)」 처럼 글자가 길고 꺾쇠까지 붙는다).
        #     ② 그 세 줄만 밴드 x0 가 <b>1085</b> 다(``IMPACT_B``·``HEALFX_B``) — 1155 가 아니다.
        #   ⚠⚠⚠ 그리고 <b>밴드를 배너 아래에서 시작해도 소용없다</b> — 130절의 「짤림 방지」가
        #     상자를 밴드 위로 <b>자라게</b> 하므로 바로 위의 배너를 물어 온다.
        #     실제로 `Impact`·`ImpactMagic` 0~2번과 `HealFx` 0~1번 프레임에 글자가 박혀 있었다.
        #
        #   ★ 좌표는 배너와 이펙트를 <b>색으로</b> 갈라 실측했다 — 배너는 회색 판 + 금색
        #     아래 테두리(rgb 218,172,128)이고 이펙트는 그 다음 줄부터 붉은/초록이 시작한다:
        #       폭발      배너 y215~236 · 이펙트 y237 부터 (붉은 178,80,78 · 최장런 33)
        #       회복      배너 y322~344 · 이펙트 y345 부터 (초록  78,92,46 · 최장런 24)
        #       투척폭발  배너 y437~459 · 이펙트 y460 부터
        ( 215,  236, 1060, 1531),   # 「일반 폭발 이펙트 (원거리/마법)」
        ( 322,  344, 1060, 1531),   # 「회복 이펙트」
        ( 437,  459, 1060, 1531),   # 「투척 폭발」 (Unused_Skill2Burst)
        ( 606,  609, 1060, 1531),
        ( 615,  619, 1060, 1531),
        ( 629,  634, 1060, 1531),
        ( 747,  750, 1060, 1531),
        ( 840,  843, 1060, 1531),
        ( 932,  936, 1060, 1531),
        ( 978,  982, 1060, 1531),
    ),
    rows=[
        # ── 몸통 ─────────────────────────────────────────────────────
        # ⚠ y0 은 <b>16</b> 이다 — 18 이면 아홉 프레임의 <b>모자 꼭대기 2px</b> 가 잘린다
        #   (실측: y16 에 17px · y17 에 78px 의 잉크가 모자 위치에 있다).
        Row("Idle",   "body",  16, 118, B0, B1, F, 9),

        # ★ 이동만 <b>좌·우 원화가 따로</b> 있다 — 미러하지 않는다.
        #
        # ★★ 칸 가르기가 ``F``(발밑)에서 <b>``bounds``</b> 로 바뀌었다 — 맨 위 1번.
        #   ⚠ 되돌리지 말 것. 망토 자락이 발밑에서 이어져 조각이 붙고, 장수를 맞추려는
        #     합치기가 «몸통 둘이 든 칸 + 날개 조각» 을 만든다.
        # ⚠⚠ 오른쪽 줄의 아래끝은 <b>315</b> 다. `warn_if_clipped` 이 «아래 118px» 이라고
        #   알려 주지만 그것은 <b>그림이 아니라 아래 「원거리 공격」 딱지 판의 왼쪽 모서리</b>다
        #   (실측: y314~319 의 잉크가 전부 ``x145~192`` 한 덩어리 · 신발은 y315 에서 끝난다).
        #   319 로 늘리면 <b>1번 프레임 밑에 금색 판 조각</b>이 박힌다 — 실제로 그렇게 나왔다.
        #   ★ 그래서 대신 그 딱지의 지움 범위를 <b>y314 부터</b>로 올렸다(``erase`` 참조).
        Row("WalkLeft",  "body", 122, 215, B0, B1, WALK_L_B, 9, folder="Walk", side="Left"),
        Row("WalkRight", "body", 224, 315, B0, B1, WALK_R_B, 9, folder="Walk", side="Right"),

        # ⚠ 딱지를 지우고 나니 <b>1번 프레임이 던진 물약</b>이 독립 덩어리로 잡혀 8개가 됐다
        #   (딱지에 붙어 있어 안 보였다). 물약은 `Projectile` 로 따로 굽고 있으므로
        #   여기서는 <b>맨 앞 한 칸을 버린다</b>(``skip``).
        #   ⚠ 아래끝 <b>404</b> 가 맞다 — y405 의 잉크는 그림이 아니라 아래 「마법」 딱지 판의
        #     모서리(``x145~183``)다. 신발은 y404 에서 끝난다. 늘리지 말 것.
        Row("RangedAttack", "body", 320, 404, B0, B1, C, 8, skip=1),
        Row("MagicAttack",  "body", 410, 501, 198, B1, C, 7),
        #   ⚠ 아래끝 <b>591</b> — y592 의 잉크는 회복이 아니라 <b>아래 스킬 2 의 구슬 장식</b>이다.
        Row("Heal",         "body", 507, 591, B0, B1, C, 7),
        #   ⚠⚠ 위끝 <b>593</b> — 608 이면 <b>3번 프레임의 모자 꼭대기(붉은 구슬)</b>가 잘린다
        #     (맨 위 ★★ 4). ⚠ 아래끝 <b>690</b> — 695 로 두면 <b>아래 「스킬 3」 줄의 날개 끝</b>
        #     (x444~478 · y693 부터 아래로 자란다)이 3·4번 프레임 밑에 조각으로 박힌다.
        Row("Skill2",       "body", 593, 690, B0, B1, C, 7),
        # ★ 「천사 강림」은 <b>날개가 위로 크게 펼쳐진다</b> — 밴드를 높게 잡았다(152px).
        #   ⚠ 아래로 넘기면 「캐릭터 기본 이미지」 딱지(y 855~)가 들어온다.
        #   ⚠ 아래 끝은 <b>846</b> 이다 — 852 로 두면 「캐릭터 기본 이미지」 딱지가 들어와
        #     프레임에 그 글자가 박힌다(구운 그림에서 확인).
        #   ★★ <b>6장</b>이다(7 이 아니다 — 맨 위 3번). ``clusters`` 가 마침 7을 내
        #     검사를 통과했고 그래서 «반쪽 + 날개만» 이 구워졌다. ``bounds`` 로 박는다.
        #   ⚠ 아래끝 <b>842</b> — 844~847 이 「캐릭터 기본 이미지」 판의 금색 테두리다.
        #   ⚠ 아래끝 <b>837</b>. 딱지 글자는 y844 부터지만 그 판의 <b>연한 후광</b>이
        #     y838 에서 이미 두 프레임을 <b>가로로 잇는다</b>(실측: y837 은 (597,655)+(717,802)
        #     두 덩어리인데 y838 은 (597,801) 한 덩어리다). 842 로 두면 4·5번 프레임 밑에
        #     <b>금색 가로선</b>이 박힌다 — 실제로 그렇게 나왔다.
        Row("Skill3",       "body", 700, 837, 55, 1046, SKILL3_B, 6, scale=False),

        # ★★ 탈진 — 「스킬3 후」(맨 위 ★★). <b>누운 자세</b>라 크기 계산이 다르다
        #   (`CharacterAnimator.ResolveScale` 이 이 구간만 상자를 눕힌다).
        #   ⚠ x1=560 은 「캐릭터 기본 이미지」 구획을 막는 벽이다.
        #   ⚠ scale=False — 누운 그림을 크기 정규화 기준에 넣으면 안 된다.
        Row("Stun",         "body", 905, 1000, 40, 560, C, 4, scale=False),

        # ── 분리 이펙트 ──────────────────────────────────────────────
        #
        # ★★ 2026-08-21 — <b>이펙트 밴드를 전부 다시 쟀다</b> (유저: *"이펙트가 안 잘리게 해
        #   윗부분 살짝 잘렸자나"*). 열한 줄 <b>전부</b> 위가 잘려 있었다 — 심한 것은 <b>33줄</b>
        #   (「투척 폭발」 472 → 439)이고 폭발·회복은 <b>22~25줄</b>이었다.
        #
        #   ⚠ 원인은 «둥근 판 테두리를 피하려고 밴드를 안쪽으로 넉넉히 넣은» 것이다.
        #     테두리는 이미 :data:`erase` 로 지우고 있으므로 <b>안쪽으로 물러설 필요가 없다</b>.
        #   ★ 값은 «그 줄의 x 범위에서 잉크가 이어지는 끝» 을 실측해 잡았다 —
        #     이웃 줄과 <b>빈 줄로 갈리는</b> 것을 확인하고 그 안쪽 끝까지만 늘렸다
        #     (예: 「하늘 레이저」 751~839 · 「폭발/섬멸」 844~931 · 사이 840~843 이 비어 있다).
        #   ⚠⚠ 옛 「하늘 레이저」 밴드는 아래끝이 <b>845</b> 여서 <b>다음 줄(폭발/섬멸)의
        #     윗부분 2줄</b>을 물고 있었다. 위아래를 동시에 재야 이런 것이 보인다.
        #
        #   ⚠⚠⚠ <b>딱지 판까지 물면 안 된다</b> — 처음에 «잉크가 이어지는 끝» 까지 그대로
        #     늘렸더니 「일반 폭발 이펙트」·「회복 이펙트」·「투척 폭발」·「천사 강림 연출」
        #     <b>네 줄의 프레임에 제목 글자가 박혀</b> 나왔다(구운 그림에서 눈으로 확인).
        #     딱지 판은 <b>가로로 200~250px 이어지는 한 덩어리</b>라 «줄마다 최장 연속 런» 을
        #     재면 이펙트(둥근 덩어리 40~120px)와 <b>명확히 갈린다</b>. 그 경계가 다음 값이다:
        #
        #       폭발  딱지 y216~237 → 밴드 <b>238</b>  (이 줄은 원래 값이 맞았다)
        #       회복  딱지 y323~345 → 밴드 <b>346</b>  (348 이었다 · 2줄 잘림)
        #       투척  딱지 y439~461 → 밴드 <b>462</b>  (472 이었다 · 10줄 잘림)
        #       천사  딱지 y635~654 → 밴드 <b>655</b>  (665 이었다 · 10줄 잘림)
        #   ★ 물약병 세 줄. 색이 곧 용도다 — 붉은=원거리 · 보라=마법 · 초록=회복
        #     (몸통 줄에서 손에 든 물약 색과 정확히 맞다).
        Row("Projectile",           "fx",  48,  98, PT0, PT1, POTION_B, 7),
        Row("Unused_PotionPurple",  "fx",  99, 148, PT0, PT1, POTION_B, 7),
        Row("Unused_PotionGreen",   "fx", 151, 196, PT0, PT1, POTION_B, 7),

        # ★ 딱지가 「일반 폭발 이펙트 (원거리/마법)」 — <b>한 그림을 두 칸에</b> 넣는다.
        Row("Impact",      "fx", 238, 306, 1085, FX1, IMPACT_B, 5),
        Row("ImpactMagic", "fx", 238, 306, 1085, FX1, IMPACT_B, 5),
        Row("HealFx",      "fx", 346, 418, 1085, FX1, HEALFX_B, 5),

        Row("Unused_Skill2Burst", "fx", 462, 527, FX0, FX1, SK2BURST_B, 4),
        # ★ 「영역 장판 (지속)」 — `skill2Fx` 는 «지면 연출» 이므로 이쪽이 맞다.
        Row("Skill2Fx",           "fx", 533, 605, FX0, FX1, SK2FX_B, 4),

        Row("Unused_Skill3Angel", "fx", 655, 743, FX0, FX1, SK3ANGEL_B, 4),
        Row("Skill3Fx",           "fx", 751, 839, FX0, FX1, SK3FX_B, 5),
        Row("Unused_Skill3Blast", "fx", 844, 931, FX0, FX1, SK3BLAST_B, 4),
    ],
    no_direction=("Projectile", "Unused_PotionPurple", "Unused_PotionGreen",
                  "Impact", "ImpactMagic", "HealFx",
                  "Unused_Skill2Burst", "Skill2Fx",
                  "Unused_Skill3Angel", "Skill3Fx", "Unused_Skill3Blast"),
    scale_reference="Idle",
    # ★★ 머리 면적으로 줄마다 크기를 맞춘다 — 맨 위 ★★ 5.
    scale_metric="min",
    # ★ 한 팔이 없는 실루엣을 지키려면 옆 칸 조각을 확실히 끊어야 한다(120절과 같은 값).
    dominant_join=0.05,
)


if __name__ == "__main__":
    run(SPEC)
