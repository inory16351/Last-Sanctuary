# 씬 반영 요청 — UI → PROTO

> `Assets/Scenes/Proto_01.unity` 는 **PROTO 단독 소유**다 (38MB, 충돌 시 복구 불가 — `UI 브렌치 준수사항.md` §3).
> UI 브랜치가 그 씬에 무언가 필요하면 **직접 고치지 말고** 이 파일에 적고 유저에게 구두로도 알린다.
>
> **작성**: UI 브랜치만 · **처리**: PROTO 브랜치 또는 머지 담당자
> 이 파일은 UI 소유다 — PROTO 는 `상태` 칸만 갱신한다.

---

## 양식 (복사해서 쓸 것)

```markdown
## 요청 N — <한 줄 제목>
- 날짜:
- 대상 씬 오브젝트:
- 요청 내용:
- 근거: (어느 패널/기능이 이걸 필요로 하는지)
- 시점: 머지 전에 필요 / 머지 후에 해도 됨
- 상태: 요청 / 유저 확인 / 반영됨(커밋 해시)
```

---

## 요청 1 — 구식 UI 오브젝트 정리 (예시 · 실제 요청으로 승격 가능)
- 날짜: 2026-08-05
- 대상 씬 오브젝트: `Energy`, `Upgrade`, `UI_Root` (Proto_01 루트/자식)
- 요청 내용: 새 HUD(`UI_HUD.unity`)가 같은 기능을 대체하므로 삭제 또는 비활성
- 근거: 새 HUD의 자원 패널·캐릭터 관리 패널이 동일 역할. 둘 다 살아있으면 화면에 겹쳐 보인다
- 시점: **머지 후**. 그 전까지 UI 브랜치는 코드로 `SetActive(false)` 처리한다 (준수사항 U-S6)
- 상태: 요청

---
<!-- 아래에 요청을 계속 추가할 것. 처리된 요청도 지우지 말고 상태만 갱신한다. -->
