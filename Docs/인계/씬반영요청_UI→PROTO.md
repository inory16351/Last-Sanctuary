# ⛔ 폐기됨 — 방향이 역전되었다

> **이 요청서는 v1 설계의 산물이고 v2(2026-08-05)에서 폐기됐다. 여기에 새 요청을 쓰지 말 것.**
>
> v1 에서는 `Assets/Scenes/Proto_01.unity` 를 **PROTO** 가 소유했기 때문에 UI 가 씬 반영을 요청해야 했다.
> v2 에서 전투 AI 가 UI 브랜치로 이관되면서 **씬 소유권이 UI 로 넘어갔고**, 이제 요청 방향은 반대다.
>
> → **`씬반영요청_PROTO→UI.md` 를 쓸 것.**
>
> 폐기 사유 전문: `UI 브렌치 준수사항.md` §4 · `프로토 브렌치 준수사항.md` §4 · `머지 계획.md` §8
>
> 파일을 지우지 않고 남겨두는 이유: 왜 없앴는지를 모르면 다음 세션이 같은 구조를 다시 만든다
> (`진행상황.md` 0절 3번의 "완료·폐기된 기록도 남긴다" 원칙).
